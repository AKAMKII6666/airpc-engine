---
# gbx / general-batch-exe 执行索引
# 用法见文首「gbx 开跑」；正式跑：node .cursor/skills/general-batch-exe/bin/gbx.js --exFile <本文件> --workdir <仓根>

batch_size: 1
max_rounds: 80
max_fix_attempts: 5
max_ineffective_fixes: 2
stop_on_fail: true
group: order

verify_default:
  - npm run typecheck

read_first:
  - AGENTS.md
  - docs/AI和人类/需求/00-文档索引.md
  - docs/AI和人类/技术设计文档/00-技术设计索引.md
  - docs/AI和人类/技术设计文档/19-引擎宿主与会话模型.md
  - .cursor/rules/codingRole.mdc
  - .cursor/rules/engine-boundaries.mdc
  - .cursor/rules/studio-boundaries.mdc

hard_stop_patterns:
  - "待确认|协议未定|需要人工裁决"
  - "引擎依赖 React|把 React 引进 rpg-engine"
  - "通话中直接 Effect|绕过 RuntimeExitCandidate"
  - "client 持 LLM Key|浏览器持 Key"
  - "实现电话壳|接入 Realtime|对标迁移旧话机|迁移 doubaoSister"

executor_extra: |
  本文件是 gbx --exFile 真源：只做「任务表」里本轮 ACTIVE／选出的 ⬜ 项。
  详细完成标准与防岔见下文对应 En 节；对照文档以该节「对照」链接为准。
  分层铁律：引擎无 React；Studio = Zustand·bis·shell；写口经 Next；通话中工具只登记 RuntimeExitCandidate。
  完成后把对应行 ⬜ 改为 ✅（只改任务表状态列与 ID 匹配行）；不要改 frontmatter 语义、不要扩大到未选中的 E*。
  不要自动 git commit。跑通该行 verify（或 verify_default）。
reviewer_extra: |
  只读审查；禁止改业务代码与需求口径。按 general-batch-exe/docs/02-review-json.md 写出 reviews/latest.json（含本轮 reviewRunId）。
  blocker/critical：漏完成标准、破坏引擎边界、通话中写 Effect、client 持 Key、未测却宣称 E4/E5/E6 闭合。
fixer_extra: |
  若 fixTrigger 含 verify：优先读 reports/latest-verify.json（errorLocations／失败命令），修到 verify 绿。
  审查 result=pass 不能当作「无活」借口。禁止顺手重构、扩 scope、改未选中任务。

workflow_dir: .ai-workflow
adapter: table
---

# 对照本仓库文档的工程落地 · 100% 计划

**工程 100% 已收口**（任务表 E0–E11 全部 ✅）。维护者可按**自己的标准**做整体效果验收，再开 1.1 体验迭代。

本文回答：**在第三步（T0–T11）主线已收口之后，还要把本仓文档要求的工程能力补到什么清单，才算「本仓库需求工程落地 100%」**——清单与代码／coverage 已对齐；不以「替签产品体验」代替维护者验收。

**不是**产品经理式「用户体验满分」承诺；**是**代码工程对照 `docs/AI和人类/需求/**` + `技术设计文档/**` 中**应由本仓（引擎 + Studio + data）承担**的条目全部落地。

| 算进本计划分母 | 不算进分母 |
|----------------|------------|
| 引擎、Studio、`data/` 内容与校验、本仓 Debug API／UI | 外仓客户端职责、模型效果品味、人工走读是否「好看」 |
| 第三步曾后置、现已纳入的缺口（世界台、画布 P1、WET、Tick、chat、资源、LLM Lore 等） | 文档从未要求本仓实现的能力 |

**分期真源（已收口）：**  
[初始](./项目初始执行索引.md)（P0–P6a）· [第二步](./项目第二步计划执行索引.md)（S0–S11）· [第三步](./项目第三步计划执行索引.md)（T0–T11）

**当前编码导航：** `AGENTS.md` → **本页（已收口）** → 维护者整体验收／1.1；回顾各条「对照文档」与完成标准。

对标 ≠ 迁移：旧话机仅能力参考。见需求 00 / 13。

---

## gbx 开跑（general-batch-exe）

本页同时是 **`gbx` 执行索引**（文首 YAML + 任务表可机器消费）。与 IDE 内 `@batch-execute` **无关**。

```bash
# 在仓根；先 dry-run 再正式跑
node .cursor/skills/general-batch-exe/bin/gbx.js \
  --exFile docs/AI和人类/里程碑/对照本仓库文档的工程落地_100计划.md \
  --workdir . \
  --dry-run

node .cursor/skills/general-batch-exe/bin/gbx.js \
  --exFile docs/AI和人类/里程碑/对照本仓库文档的工程落地_100计划.md \
  --workdir .
```

- 进度真源：下方 **「任务表」** 的 `⬜`→`✅`（详情节里的条目是给人／Agent 的完成标准，不替代任务表勾选）
- 工作流产物：`--workdir` 下 `.ai-workflow/`（STATE、reviews、logs）
- 无 cursor-agent 联调 FSM：加 `--mock-agent`（请用副本，避免污染真索引）
- 需要 git checkpoint：显式 `--checkpoint`（脏树会 BLOCKED）

---

## 0. 执行防岔（必读）

1. **一层一刀：** 下列 E* 为工程包；未列入的 UI 微调、文案润色不进本计划。  
2. **真源不漂移：** Content 在 `data/` JSON；经历态在用户 Profile + Memory SQLite；CallSession 在 Host 内存。世界台改的是 **Profile.world／schedule**，不是另起一套世界库。  
3. **画布真源：** 复制／布局／搜索只动 `.s-card.json` + `story.conf` + `canvas.layout.json`；出口语义仍在 `exits[].effects`，边仍是投影。  
4. **调试铁律：** 通话中工具只登记 `RuntimeExitCandidate`；不直接 Effect；不改本通 `frozenCard`。  
5. **分层：** 引擎无 React；Studio = Zustand · bis · shell；写口经 Next；client 无引擎写口、无 LLM Key。  
6. **完成即勾：** 任务表对应行满足该节完成标准后 ⬜→✅；全部 ✅ 后页顶改为「工程 100% 已收口」，再交维护者整体验收／1.1。  
7. **验收主权：** 勾选表示**工程对照文档已落地**；维护者可用自己标准打回改行为，但不把「没测好看」当成「计划漏项」。

---

## 总序

```text
基线   P0–P6a / S0–S11 / T0–T11（已收口，不重做）

E0  索引／AGENTS／与第三步结转说明
E1  世界台（Profile.world 可读可写）
E2  画布 P1（搜索／复制粘贴／自动布局）
E3  资源台 + assets 全链路 + 08 warning／ASSET_URI_MISSING 收齐
E4  加深测与样例（refer E2E、A→B、隔离／recurring、night layer）
E5  Memory vignette 写入 + 真 rollup
E6  Lore LLM bootstrap（失败仍 fallback）
E7  文本对话调试（chat + server Adapter）
E8  调试 SSE 最小推送
E9  Clock／日常 Tick 模拟器级
E10 完整 WET 查询／受控追加／重放视图
E11 仓内文档与 coverage 对齐收口
```

**推荐刀序：** E0 → E1∥E2 → E3 → E4∥E5 → E6 → E7∥E8 → E9 → E10 → E11。  
（`batch_size: 1`：每轮只取一张表；并行意图由维护者／Executor 自行交错，勿一次塞两期。）

当前：**工程 100% 已收口**（E0–E11 全部 ✅）。维护者开始整体验收 → 提 1.1。

---

## 1. 任务表（gbx 真源）

| 状态 | ID | 任务 | verify |
|------|-----|------|--------|
| ✅ | E0 | 计划挂载与结转：AGENTS／第三步指针／本页总序齐备 | — |
| ✅ | E1 | 世界台：/world + Lore／Facts／knowledge／schedule 读写（见 E1 节） | npm run typecheck |
| ✅ | E2 | 画布 P1：搜索／复制粘贴／自动布局（见 E2 节） | npm run typecheck |
| ✅ | E3 | 资源台 + assets 链路 + ASSET_URI_MISSING／08 warning coverage（见 E3 节） | npm run typecheck |
| ✅ | E4a | refer E2E：invokeTool(refer_to_expert)→endCall→unlock+schedule／pending | npm test |
| ✅ | E4b | A→B：角色 A 出口写 B 的 knowledge 或 memory（样例包+测） | npm test |
| ✅ | E4c | 双 userId 隔离 + recurring 意图写入／调试语义测 | npm test |
| ✅ | E4d | night／late_night 层样例 + Composer／host 测 | npm test |
| ✅ | E5 | Memory vignette 写入+投影；rollupIfNeeded 真实现+测 | npm test |
| ✅ | E6 | Lore 真 LLM bootstrap（server Key；失败→fallback；mock port 测） | npm test |
| ✅ | E7 | 文本对话调试：debug chat API + UI；Story／Free 多轮→endCall | npm run typecheck |
| ✅ | E8 | 调试 SSE 最小推送；文档不再写「仅 refreshStamp」 | npm run typecheck |
| ✅ | E9 | Clock／日常 Tick 模拟器：API+入口；once+recurring+隔离测 | npm test |
| ✅ | E10 | 完整 WET：查询 UI／受控追加／重放视图 | npm run typecheck |
| ✅ | E11 | 文档与 coverage 对齐收口；页顶「工程 100% 已收口」 | npm run typecheck |

### 批次出口（每批／每轮）

- 本批任务表行均为 ✅，且该节**完成标准**条条满足  
- 对应 `verify`（或 `verify_default`）exit 0  
- Reviewer：`.ai-workflow/reviews/latest.json` 中 `blocker=0` 且 `critical=0`，且带本轮 `reviewRunId`

---

## E0 · 计划挂载与结转

**目标：** 执行入口唯一指向本页；第三步「后置」中已并入本计划的项不再沉默悬挂。

**对照：** 本页 · [第三步](./项目第三步计划执行索引.md) · `AGENTS.md`

**是什么：** 文档／导航卫生，无业务 runtime。

**完成标准：**

- `AGENTS.md` 必读指向本页为**当前工程刀口**
- 第三步页顶注明：后置缺口中并入本计划的项以**本页为准**继续
- 本页总序与各 E* 完成标准齐备

---

## E1 · 世界台（完整世界模块）

**目标：** Studio 有独立「世界」台：对着**当前选中用户**的 Profile，看／改世界状态。

**对照：** [40](../需求/40-世界柱.md) · [03](../需求/03-编辑器界面需求.md) · [10](../需求/10-用户与存档.md) · [19](../技术设计文档/19-引擎宿主与会话模型.md) §12 Lore

**是什么：**

| 做 | 不做（本条防岔） |
|----|------------------|
| 路由如 `/world`；Nav 入口；依赖 UserGate | 完整「过一整天」模拟器（→ E9） |
| 读／写 `Profile.world.lore`／`facts`／`knowledge` | 另建全局世界 DB |
| 展示／谨慎编辑 `schedule`（clockMs、intents） | 社交图编辑（属角色台） |
| 「重生成 Lore」调 Host bootstrap（fallback 或 E6 LLM） | WET 完整编辑器（→ E10）；本条可链到日志只读 |

**完成标准：**

- `/world`（或等价）可进入；未选用户则 UserGate
- Lore：展示 source／premise／perspectives；可手改保存；可触发 bootstrap／重生成
- Facts：列表增删改（字段对齐 40 `WorldFact`）；经 Zod／门面写 Profile
- knowledge：按 agentId 维护已知 factId 列表并保存
- schedule：只读或有限编辑 intents／clock 展示；保存走 Profile
- bis／shell／ajax 分层；ensureProfile／save 与现 Host 一致

---

## E2 · 画布 P1（搜索／复制粘贴／自动布局）

**目标：** 完善**现有**故事画布编辑效率，不改 CallCard 数据模型。

**对照：** [03 §9](../需求/03-编辑器界面需求.md)

**是什么：**

| 能力 | 具体行为 |
|------|----------|
| **搜索** | 按 cardId／title／ownerAgentId 过滤或定位；选中 + 画布聚焦 |
| **复制／粘贴** | 选中卡 → 新 cardId + 深拷贝定义 + layout 节点偏移；更新 conf.cards；进撤销栈 |
| **自动布局** | 一键按泳道／简易分层重排 **仅 layout**；可撤销 |

**防岔：**

- 不改 `exits[].effects` 语义去「自动修边」（复制后旧出口指向：v1 须写死——保留指向或清空，二选一）
- 不做跨包粘贴

**完成标准：**

- 搜索可定位到节点
- 单卡复制粘贴保存重载仍在；多选复制可分期但须在本条注明
- 自动布局只写 `canvas.layout.json`；撤销可用

**本期写死／分期注明：**

- **复制后出口指向（v1）：保留指向**（深拷贝 exits／effects，不改写目标 cardId，不清空）
- **多选复制粘贴：分期**（本条仅 `selectedCardId` 单卡；多选批量复制后续刀）
- **跨包粘贴：不做**（包内内存剪贴板，packageId 不一致拒绝粘贴）

---

## E3 · 资源台 + assets 全链路 + 08 校验收齐

**目标：** 资源可在 Studio 维护；校验对缺失 URI／文档 warning 不再大片 deferred。

**对照：** [14](../需求/14-资源与媒体资产.md) · [08](../需求/08-内容校验规则.md) · `VALIDATE_PACKAGE_ERROR_COVERAGE`

**是什么：**

- **资源台**（如 `/assets`）：登记／列表／删除元数据与文件（约定根如 `data/assets/`）
- **卡引用：** playbackClipId 等可从资源列表选择
- **校验：** `ASSET_URI_MISSING` 进入覆盖（error 或文档定级后硬拦）；08 **warning** 主路径进 coverage.covered

**防岔：** 扬声器真播可仍为桩；本条要的是**文件存在性与引用一致性**。

**完成标准：**

- 资源台 CRUD（或登记）可用
- 至少一条样例卡引用有效相对路径；缺文件时 validate 报告约定 ruleId
- coverage 更新：`ASSET_URI_MISSING` + 本期收齐的 warning 列表写入文档／常量

---

## E4 · 加深测与内容样例

**目标：** 能力组件已在、缺「整通断言／样例」的条目补齐。

**对照：** [06](../需求/06-验收标准.md) · [13](../需求/13-工具与能力注册.md) · [12](../需求/12-记忆模型.md) · [50](../需求/50-对话与模型适配.md)

| 子项 | 是什么 | 完成标准（对应任务表 ID） |
|------|--------|---------------------------|
| **E4a refer E2E** | `invokeTool(refer_to_expert)` → endCall → unlock + schedule／pending | host 测一条整通 |
| **E4b A→B** | 角色 A 出口写 B 的 knowledge 或 memory | 独立小包或扩 golden + 测 |
| **E4c 隔离／recurring** | 双 userId 隔离；recurring 意图写入与本仓调试语义有测 | 测覆盖两条 |
| **E4d night layer** | 内容含 `night`／`late_night` 层；改本地时间命中 | 样例 JSON + Composer／host 测 |

**防岔：** 不借此返工无关编辑器 UX。

---

## E5 · Memory vignette + 真 rollup

**目标：** 记忆契约升到 12／20 要求的可运行行为。

**对照：** [12](../需求/12-记忆模型.md) · [20](../技术设计文档/20-记忆存储与投影.md)

**是什么：**

- **vignette：** Commit（或约定路径）可写入 `kind=vignette` 并进入投影预算
- **rollup：** `rollupIfNeeded` **非空**：达阈值时压缩／汇总旧 episodic，有测

**防岔：** 不做向量检索；继续 FTS／现有限。  
**口径：** vignette 本批闭合在 **端口契约 + Commit 可写 + 投影测**；挂机总入口（Free PostPipeline）默认仍只写 `call_summary`，**LLM／Commit 抽取 vignette 接线后置**（E7 调试对话或后续抽取）。

**完成标准：**

- vignette 有写有投影测
- rollupIfNeeded 有实现 + 测（触发前后可断言；含跨月／跨季缺失补算）

---

## E6 · Lore 真 LLM bootstrap

**目标：** `LoreBootstrapPort` 可走真模型；失败仍 fallback（T10 已具备）。

**对照：** [40 §2](../需求/40-世界柱.md) · [19 §12](../技术设计文档/19-引擎宿主与会话模型.md)

**是什么：**

- Server 注入 LLM 适配（Key 仅 server）
- 输入：user.location + CharacterDef 列表 → 合法 `WorldLoreDoc`，`source: "llm"`
- 失败／无 Key → `buildFallbackLore`；日志；不挡建用户
- 世界台（E1）可区分 LLM 与 fallback

**防岔：** 禁止 client 调厂商 API；完成标准是**结构化写入成功 + source 正确**，不是文案品味。

**完成标准：**

- 可配置启用 LLM port；单测可用 mock port
- 失败路径测：抛错 → fallback 仍写入 Profile

---

## E7 · 文本对话调试（50 v1.1）

**目标：** 调试台可多轮文本对话（Manual 挂机之外）。

**对照：** [50](../需求/50-对话与模型适配.md) · [04](../需求/04-调试器界面需求.md) · [18](../技术设计文档/18-部署拓扑与BS架构.md)

**是什么：**

- `POST` 和／或流式 `/api/debug/chat`
- Server 侧 TextDialogueAdapter（或等价）：Composer 结果、调模型、回传助手文本
- 调试 UI：聊天面板；工具走现有 invoke；挂机仍 Outcome／Free 管线
- 通话中不直接 Effect；playtest 仍藏 private（T9）

**完成标准：**

- Story 与 Free 各至少一测或可脚本路径：多轮 chat → endCall
- UI 可看见用户／助手轮次；错误码经门面

---

## E8 · 调试 SSE 最小推送

**目标：** 会话／日志变化可推送到浏览器。

**对照：** [04](../需求/04-调试器界面需求.md) · [18](../技术设计文档/18-部署拓扑与BS架构.md) · [50](../需求/50-对话与模型适配.md)

**是什么：**

- 最小 `text/event-stream`（snapshot 脏通知和／或 chat 流）
- 客户端订阅；可保留 stamp 兜底

**防岔：** 不做完整事件总线产品化。

**完成标准：**

- 至少一条调试相关流可推达（挂机／chat／snapshot 之一）
- 文档不再写「SSE 后置、仅 refreshStamp」

---

## E9 · Clock／日常 Tick（模拟器级）

**目标：** 可推进逻辑时钟；once／recurring 在本仓可观测闭合。

**对照：** [40 §5](../需求/40-世界柱.md) · [10](../需求/10-用户与存档.md) · 现有 `advanceClock`

**是什么：**

- API／UI：快进、跳到时刻、「推到下一意图」等
- **once：** 到期 → pending／可 outbound
- **recurring：** 写清并实现本仓调试语义（如到期生成可观测 once 实例）；禁止只写 intents 却宣称 Tick 完成
- 双用户隔离测（可与 E4c 合并）

**防岔：** 是 **Profile.schedule 模拟器**，不是真实闹钟守护进程。

**完成标准：**

- Tick API + 调试或世界台入口
- once + recurring 均有测；隔离有测

---

## E10 · 完整 WET（查询／受控追加／重放）

**目标：** 逻辑事件可查、可受控追加、可看重放说明。

**对照：** [40](../需求/40-世界柱.md) · [16](../需求/16-错误处理与幂等事务.md) · [19 §14](../技术设计文档/19-引擎宿主与会话模型.md)

**是什么：**

| 能力 | 说明 |
|------|------|
| 存储 | 定稿 logs／wet 与 Profile 关系（短注即可） |
| 查询 UI | 世界台或调试「事件」：按 type／session／时间过滤 |
| 受控追加 | 补偿／标注类；**禁止**任意篡改历史破坏幂等 |
| 重放视图 | 只读：关联 exit／effect plan／session 摘要 |

**完成标准：**

- 查询 UI 可用
- 追加路径有校验；有测或脚本
- 重放视图至少挂接 effect plan／exit 一类观测

---

## E11 · 仓内文档与 coverage 对齐收口

**目标：** 清单与代码、coverage、里程碑指针一致。

**对照：** [06](../需求/06-验收标准.md) · 08 coverage · 各里程碑索引

**是什么：**

- 本页全部 E* ✅ → 页顶「工程 100% 已收口」
- 06／coverage 与实现同步（**工程勾选**）
- 第三步中已被本计划消化的「后置」句改为指向本页已完成

**防岔：** 不替维护者做产品验收签字；只保证文档不说谎。

**完成标准：**

- [x] 本页 E0–E10 完成标准已勾（任务表 ✅）
- [x] `AGENTS.md` 更新为「工程 100% 已收口」（或指向下一分期若有）
- [x] coverage／06 工程相关项已消化或显式标「非本仓分母」（`VALIDATE_PACKAGE_ERROR_COVERAGE.deferred` 空；[06](../需求/06-验收标准.md) E11 收口）

---

## 完成定义（何时开始你的整体验收）

```text
任务表 E0–E11（含 E4a–E4d）全部 ✅
  → 本仓库「对照文档的工程落地」= 本计划定义的 100%
  → 维护者按自己的标准做整体效果验收
  → 验收改点进入 1.1（另开列表）
  → 仅当实现未满足上表某条完成标准时，才算本计划漏交付
```

---

## 与 1.1 的边界

| 本计划内 | 1.1（整体验收后提出） |
|----------|------------------------|
| 上表能力从无到有／从半到齐 | UX、字段编排、文案、流程顺手 |
| 测与样例证明工程契约 | 剧情包内容创作 |
| 文档与代码一致 | 超出上表的新能力 |

---

## 维护

- **gbx：** dry-run → 正式跑；进度只改「任务表」状态列  
- 开跑：确认 E0 ✅；页顶／总序写「当前：En」  
- 完成一期：勾任务表；更新 AGENTS 当前刀口  
- 歧义：先对齐维护者，再改需求／技术设计，最后改本计划（含 frontmatter／任务表）  
- 编码：`AGENTS.md` → 本页当前 ⬜ → 该条对照文档  
