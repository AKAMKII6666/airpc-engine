---
# Studio V2.0 · 细化修改 4 执行索引（gbx / general-batch-exe）
# 用法：node .cursor/skills/general-batch-exe/bin/gbx.js --exFile <本文件> --workdir <仓库根> [--dry-run]
# 本轮目标：故事编辑器去重角色入口 → 画布角色 A1 → 通话卡对齐 CallCardDefinition（mock）→ 包/资源浮窗。
# 前置：细化修改 1–3 已收口；AutoForm + 角色/资源库 bis 可用。

batch_size: 2
max_rounds: 50
max_fix_attempts: 5
max_ineffective_fixes: 2
stop_on_fail: true
group: order

verify_default:
  - npm run quality:studio

read_first:
  - AGENTS.md
  - .cursor/rules/codingRole.mdc
  - .cursor/rules/studio-boundaries.mdc
  - .cursor/rules/studio-v2-quality.mdc
  - docs/AI和人类/里程碑/v2.0/细化修改4_需求.md
  - docs/AI和人类/里程碑/v2.0/细化修改1_需求.md
  - docs/AI和人类/里程碑/v2.0/第二步需求.md
  - docs/AI和人类/sudioV2.0版本/需求/ui/02-新版故事编辑器界面UI设计向导稿.md
  - docs/AI和人类/sudioV2.0版本/需求/ui/09-Studio-V2前端目录结构与落点规则.md
  - docs/AI和人类/sudioV2.0版本/需求/ui/08-Studio-V2前端工程规则与质量门禁需求.md
  - packages/rpg-engine/src/schema/callCard.ts

hard_stop_patterns:
  - "协议未定|需要人工决策|阻塞实现"
  - "恢复 CharacterAnchorRail|恢复左侧角色轨|恢复底栏角色浮窗|恢复 CharacterPickerFloat"
  - "通话卡写盘|写入 storis-packages|story\\.conf\\.json 写盘|Host 写口|EngineHost"
  - "改 packages/rpg-engine|CallCardDefinitionSchema 变更|引擎 schema 修改"
  - "校验态.*(Select|可编辑)|validation.*Select.*校验|exitCount.*(手填|TextField)|goalOneLiner.*(TextField|AutoForm|可编辑)|entryLabel.*(TextField|自由文本)"
  - "重复实现角色表单|新建 addEditorCharacter|平行 asset 表单"
  - "调试器改版|改版 DebuggerShell"
  - "包配置写进通话卡属性|单卡属性内嵌包配置"

executor_extra: |
  只实现当前 ACTIVE / 本轮选出的 ⬜ 行；完成后把对应行改为 ✅。
  需求真源：细化修改4_需求.md。刀序 S4-1 → S4-9；严格 group: order。
  通话卡/包/画布布局：仅会话 mock，禁止写 data/storis-packages。
  角色新增编辑：复用 /characters 的 FormModal + bis，可写 data/characters。
  资源 CRUD：复用 /assets bis，禁止 duplicate。
  字段对齐 CallCardDefinition；context.objective 替代 goalOneLiner；entryMode 替代 entryLabel。
  校验态仅节点 badge，不进属性 Formik。
  出口 Handle 动态 + MUI Tooltip（名称+概要）。
  不要改本索引 frontmatter 语义；不要顺手改引擎或调试器。

reviewer_extra: |
  只读审查；禁止改业务代码。写出带本轮 reviewRunId 的 reviews/latest.json（见 general-batch-exe/docs/02-review-json.md）。
  对照 细化修改4_需求.md §2–§5。
  blocker/critical：仍保留左侧轨或底栏角色浮窗、通话卡/包写盘、引擎改动、校验态可编辑、重复角色/资源表单、占位区未改真实模块、quality:studio 失败。

fixer_extra: |
  只根据审查报告与 latest-verify.json 修确认成立的问题；禁止顺手重构无关模块。
  不得用删注释、压行、大范围 eslint-disable、门禁 allowlist 掩盖结构问题。

workflow_dir: .ai-workflow-v2-refine4
adapter: table
---

# Studio V2.0 · 细化修改 4 执行索引

> **用法：**  
> `node .cursor/skills/general-batch-exe/bin/gbx.js --exFile docs/AI和人类/里程碑/v2.0/细化修改4_执行索引.md --workdir . --dry-run`  
> 通过后再去掉 `--dry-run` 正式跑。  
> **独立 workflow：** `.ai-workflow-v2-refine4/`（勿与 refine1–3 / step2 混用）。  
> **前置：** 细化修改 1–3 任务已 ✅；AutoForm 与角色/资源库 bis 可用。

本文回答：**故事编辑器 UI 收口按什么顺序做、做到哪算完**。  
需求真源：**[细化修改4_需求.md](./细化修改4_需求.md)**。  
人类讨论：[原始需求讨论记录](../../../人类/原始需求讨论记录)（约 764–778 行）。

---

## 0. 已钉死口径

### 0.1 范围边界

| 做 | 不做（本轮） |
|----|----------------|
| 删左侧角色轨 + 底栏角色浮窗 | 恢复任一重复角色列表 |
| 画布角色 A1：复用角色库 FormModal | 编辑器内平行角色表单契约 |
| 通话卡属性对齐 CallCardDefinition（mock） | 通话卡/包写盘、Host |
| 包配置独立只读浮窗 + docker icon | 包字段用户可编辑 |
| 资源浮窗 CRUD 复用 assets | 复制 asset bis |
| 出口动态 Handle + Tooltip | ExitCondition 完整树编辑器 |
| 占位改真实表单模块 | 保留「占位」纯文案折叠区 |

### 0.2 与 refine1–3 的依赖

- 必须已有 AutoForm、`FormModal`、`PromptSceneListEditor`。
- 必须已有 `/characters`、`/assets` 详情/新建 bis。
- **不** 要求 refine3 schedule API；故事编辑器本轮不编 Profile.schedule。

### 0.3 推荐刀序

```text
S4-1  删重复角色入口 + 壳层清理
S4-2  画布角色新增/编辑（A1 复用角色库）
S4-3  通话卡投影类型 + 枚举 labels
S4-4  属性浮窗加宽 + AutoForm 真字段（去 debug 可编辑项）
S4-5  出口动态 Handle + Tooltip
S4-6  占位改真实表单模块（promptScenes/exits/toolPolicy/schedule）
S4-7  包配置浮窗 + 底栏调整
S4-8  资源浮窗 CRUD 复用 assets
S4-9  文档同步 + 讨论记录
```

`group: order`：严格按表从上到下取 ⬜。

> **gbx 表解析注意：** 任务单元格勿写英文 `status`、勿写「状态」子串；避免同行同时触发 `id` 子串与「任务」误判。

### 0.4 引擎/写盘声明（全批）

```text
行为目标：Studio V2 故事编辑器 UI mock 收口；字段名对齐 CallCardDefinition 只读引用
目标模块：apps/studioV2/src/pageComponents/storyEditor/**、bis/pageBis/storyEditor/**、typeFiles/story/**
允许修改：上述 + tests/storyEditor/** + docs（本批列明）
禁止落点：packages/rpg-engine/**（schema/运行时）、data/storis-packages 写盘、Host/API 新 save 端点
状态不变量：通话卡「应用到画布」仅会话内；角色/资源可走已有 characters/assets API
verify：npm run quality:studio
```

---

## 1. 任务表

| 状态 | ID | 任务 | verify |
|------|-----|------|--------|
| ✅ | V2-S4-1 | **删重复角色入口**：移除 CharacterAnchorRail、CharacterPickerFloat、底栏角色 icon；StoryEditorShell 与 mock 清理；保留画布 CharacterAnchorFlowNode | npm run quality:studio |
| ✅ | V2-S4-2 | **画布角色 A1**：锚点节点可选中；新增/编辑复用角色库 FormModal + bis（写 data/characters）；删或收敛 addEditorCharacterForm | npm run quality:studio |
| ✅ | V2-S4-3 | **通话卡投影对齐引擎**：EditorCallCardProjection（或等价）+ callCardLabels；mock 图/selection 去 goalOneLiner/entryLabel/validation 可编辑语义 | npm run quality:studio |
| ✅ | V2-S4-4 | **属性浮窗 AutoForm 真字段**：加宽 380–420px；title/entryMode/interactionMode/context.objective 等；只读 cardId/ownerAgentId；去掉校验态与 exitCount 表单项 | npm run quality:studio |
| ✅ | V2-S4-5 | **出口动态 + Tooltip**：CallCardFlowNode 按 exits[] 渲染 Handle；MUI Tooltip 显示出口名称与概要 | npm run quality:studio |
| ✅ | V2-S4-6 | **子模块表单化**：promptScenes（PromptSceneListEditor）、exits 列表块、toolPolicy、schedule 条件块；移除「占位」纯文案 details | npm run quality:studio |
| ✅ | V2-S4-7 | **包配置浮窗**：PackageConfigFloat 只读 StoryPackageConf；底栏新增包 icon、去掉角色 icon；从 NodePropertyForm 移除包配置折叠 | npm run quality:studio |
| ✅ | V2-S4-8 | **资源浮窗 CRUD**：AssetPickerFloat 复用 assets FormModal/bis，支持增删改 | npm run quality:studio |
| ✅ | V2-S4-9 | **文档同步**：细化修改4 勾选验收；向导稿 02 §3.2/§12；第二步 E4/E5 supersede；讨论记录追加结论 | — |

---

## 2. 批次出口（每批结束后）

- 本批任务行均为 ✅
- 任务 `verify` / `verify_default` exit 0
- Reviewer：`{workflow_dir}/reviews/latest.json` 中本轮 `blocker=0` 且 `critical=0`

额外关注：

- **S4-1**：是否仍 import CharacterAnchorRail / CharacterPickerFloat
- **S4-2**：是否仍用 addEditorCharacter 平行表单；角色字段是否与 /characters 一致
- **S4-4**：属性窗是否仍有 validation Select、exitCount 输入、包配置折叠
- **S4-5**：是否仍写死 exit_a/exit_b 且无 Tooltip
- **S4-7**：包配置是否仍在单卡属性内
- **S4-8**：资源浮窗是否复制 asset 字段定义

---

## 3. 本轮总出口（全部 ✅ 后）

- [x] [细化修改4_需求.md](./细化修改4_需求.md) §5 可勾选
- [x] `npm run quality:studio` 全绿
- [x] 故事编辑器角色入口仅画布节点；底栏仅资源+包配置
- [x] 通话卡字段对齐 CallCardDefinition mock；无 debug 可编辑项
- [x] 未写 storis-packages；未改引擎；未改调试器

收口后 **不要在本文件继续加任务**。

---

## 4. 任务 ↔ 需求映射

| 任务组 | 细化修改4_需求 | 其它 |
|--------|----------------|------|
| V2-S4-1 | §2.1 · §2.2 | StoryEditorShell · BottomToolBar |
| V2-S4-2 | §2.1 · §3 复用清单 | characters bis |
| V2-S4-3 · S4-4 · S4-5 · S4-6 | §2.5 · §2.6 · §2.8 | callCard.ts 只读 |
| V2-S4-7 | §2.2 · §2.3 | StoryPackageConfSchema |
| V2-S4-8 | §2.4 · §3 | assets bis |
| V2-S4-9 | §4 · §5 | 02 向导稿 · 第二步 E4/E5 |

---

## 5. 给执行者的提醒

- **先 `--dry-run`** 确认索引可解析，再正式跑 gbx。
- 通话卡本轮 **只 mock**；「应用到画布」= 会话内节点 data，禁止伪造「已保存到磁盘」toast。
- `context.objective` 是「本轮目标」真字段名；不要保留 `goalOneLiner`。
- 章节节点勿与 CallCard 混用同一 debug 字段集。
- 需要 git checkpoint：`gbx ... --checkpoint`（脏树会 BLOCKED）。
