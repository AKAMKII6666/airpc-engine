---
# Studio V2.0 · 细化修改 5 执行索引（gbx / general-batch-exe）
# 用法：node .cursor/skills/general-batch-exe/bin/gbx.js --exFile <本文件> --workdir <仓库根> [--dry-run]
# 本轮目标：章节结束可配 · Effect 枚举下拉 · 工具策略 allowlist 多选 · 枚举审计修复
batch_size: 2
max_rounds: 60
max_fix_attempts: 5
max_ineffective_fixes: 2
stop_on_fail: true
group: order

verify_default:
  - npm run quality:studio

read_first:
  - AGENTS.md
  - .cursor/rules/codingRole.mdc
  - .cursor/rules/studio-v2-quality.mdc
  - .cursor/rules/testing-and-quality.mdc
  - docs/AI和人类/里程碑/v2.0/细化修改4_需求.md
  - docs/AI和人类/里程碑/v2.0/细化修改5_需求.md
  - docs/AI和人类/sudioV2.0版本/需求/ui/02-新版故事编辑器界面UI设计向导稿.md
  - packages/rpg-engine/src/schema/outcome.ts
  - packages/rpg-engine/src/tools/builtinRegistry.ts

hard_stop_patterns:
  - "恢复 CharacterAnchorRail|恢复左侧角色轨|恢复底栏角色浮窗|恢复 CharacterPickerFloat"
  - "通话卡/包/画布写盘到 data/storis-packages|写入 story\\.conf\\.json|Host save|EngineHost 写口"
  - "在属性浮窗把校验态或 exitCount 作为用户可编辑输入/下拉"
  - "让 allowedToolIds 在 allowlist 模式仍为自由文本/字符串列表手填 toolId"
  - "让 exits.effects 的 effect 仍作为自由文本输入"
  - "改 packages/rpg-engine|CallCardDefinition|EffectSchema|ToolRegistry schema"
  - "调试器改版|修改 DebuggerShell"
  - "复制/新建平行角色或资源表单而不复用 bis"

executor_extra: |
  只实现当前 ACTIVE / 本轮选出的 ⬜ 行；完成后把对应行改为 ✅。
  需求真源：细化修改5_需求.md。
  刀序 S5-1 → S5-5；严格 group: order。

  行为与范围：
  - 章节结束配置与出口 Effect / 工具策略仅做 UI mock 投影；不得写盘、不得接 Host save。
  - exits.effects.effect 必须由枚举下拉写入，禁止自由文本。
  - toolPolicy.allowedToolIds 必须由“内置工具选项的多选控件”写入，禁止继续用 allowlist 下的 StringListEditor 自由手填。

  质量：
  - 枚举/中文文案的来源统一收口到 typeFiles（例如 callCardLabels 的等价位置），不要在组件里散落 schema 原文。
  - 需要更新 node/projection 类型时，同步更新相关会话 mock（如 CanvasMockGraph）。

reviewer_extra: |
  只读审查；禁止改业务代码。写出带本轮 reviewRunId 的 reviews/latest.json（见 general-batch-exe/docs/02-review-json.md）。
  对照 细化修改5_需求.md：
  - S5-1：chapter_end 选中后能配置 nextPackageId/nextEntryCardId
  - S5-2：exits.effects.effect 为下拉且仅允许已知枚举值
  - S5-3：toolPolicy.allowlist 模式下为多选内置工具列表，非 allowlist 模式隐藏或禁用
  - S5-4：枚举审计通过（角色/玩家管理无枚举 TextField/StringListEditor 残留；故事编辑器重点枚举字段已为枚举控件）
  blocker/critical：缺能力入口、枚举控件未落地、允许自由文本填枚举、写盘/引擎改动、quality:studio 失败。

fixer_extra: |
  只根据审查报告与 latest-verify.json 修确认成立的问题；禁止顺手重构无关模块。
  不得用删注释、压行、大范围 eslint-disable、门禁 allowlist 掩盖结构问题。

workflow_dir: .ai-workflow-v2-refine5
adapter: table
---

# Studio V2.0 · 细化修改 5 执行索引

> **用法：**
> `node .cursor/skills/general-batch-exe/bin/gbx.js --exFile docs/AI和人类/里程碑/v2.0/细化修改5_执行索引.md --workdir . --dry-run`
> 通过后再去掉 `--dry-run` 正式跑。
>
> **独立 workflow：** `.ai-workflow-v2-refine5/`

本文回答：**故事编辑器 UI 对“章节结束/Effect/工具策略枚举”补齐按什么顺序做、做到哪算完**。  
需求真源：**[细化修改5_需求.md](./细化修改5_需求.md)**。  
人类讨论：[原始需求讨论记录](../../../人类/原始需求讨论记录)（约 813–818 行）。

---

## 0. 已钉死口径

### 0.1 范围边界

| 做 | 不做（本轮） |
|---|---|
| chapter_end 配置字段（mock） | 通话/包/画布写盘到 `data/storis-packages` |
| exits.effects.effect 枚举下拉（mock） | 整理/改引擎 schema |
| toolPolicy.allowlist 内置工具多选（mock） | Host/EngineHost save 接入 |
| 枚举审计修复（Studio UI） | 退出条件树完整编辑器 |

### 0.2 推荐刀序

```text
S5-1 章节结束点可配置（nextPackageId / nextEntryCardId）
S5-2 出口 effects.effect：枚举下拉 + 中文名
S5-3 toolPolicy.allowedToolIds：内置工具多选控件
S5-4 枚举审计：补齐遗留枚举控件
S5-5 文档/讨论记录同步与验收勾选
```

group: order：严格按表从上到下取 ⬜。

> **gbx 表解析注意：** 任务单元格勿写英文 `status`、勿写「状态」子串；避免同行同时触发 `id` 子串与“任务”误判。

---

## 1. 任务表

| 状态 | ID | 任务 | verify |
|---|---|---|---|
| ✅ | V2-S5-1 | **章节结束节点属性可配置**：选中 `chapter_end` 后提供 `nextPackageId` 与 `nextEntryCardId`（Select 下拉）；package 变更后 entry 卡集合随之变化 | npm run quality:studio |
| ✅ | V2-S5-2 | **出口 Effect 枚举下拉**：新增 `EFFECT_NAME_OPTIONS`（中文映射）并把 `exits.effects[].effect` 从 TextField 改为 Select；禁止自由文本 effect | npm run quality:studio |
| ✅ | V2-S5-3 | **工具策略 allowlist 多选**：替换 `toolPolicy.allowedToolIds` 的 StringListEditor 手填方式；实现多选内置工具列表写回 toolId[]；非 allowlist 隐藏或禁用 | npm run quality:studio |
| ✅ | V2-S5-4 | **枚举审计修复**：故事编辑器重点枚举字段完成枚举控件落地；角色/玩家管理枚举字段保持 Select/Radio/等价控件（无枚举 TextField/StringListEditor 残留） | npm run quality:studio |
| ✅ | V2-S5-5 | **文档同步**：在 `细化修改5_需求.md` 验收清单勾选；确认向导稿 02 与讨论记录对齐 | — |

---

## 2. 批次出口（每批结束后）

- 本批任务行均为 ✅
- 任务 `verify` / `verify_default` exit 0
- Reviewer：`{workflow_dir}/reviews/latest.json` 中本轮 `blocker=0` 且 `critical=0`

---

## 3. 本轮总出口（全部 ✅ 后）

- [x] 章节结束点选中后可配置 `nextPackageId` 与 `nextEntryCardId`
- [x] exits.effects.effect 为枚举下拉，用户只能选已知枚举值（如 `keep_card_pending`）
- [x] toolPolicy.allowlist 模式下 allowedToolIds 为内置工具多选列表，非允许模式下隐藏/禁用；禁止自由文本/字符串列表手填
- [x] 枚举审计通过：角色/玩家管理与故事编辑器无“枚举却用自由文本”的残留
- [x] `npm run quality:studio` 全绿

收口后 **不要在本文件继续加任务**。

---

## 4. 任务 ↔ 需求映射

| 任务组 | 细化修改5_需求 | 其它 |
|---|---|---|
| V2-S5-1 | §2.1 | 向导稿 02 §章节结束点第一版 |
| V2-S5-2 | §2.2 | `packages/rpg-engine/src/schema/outcome.ts` |
| V2-S5-3 | §2.3 | `packages/rpg-engine/src/tools/builtinRegistry.ts` |
| V2-S5-4 | §2.4 | role/player management 现有 Select 契约 |
| V2-S5-5 | §4 · §5 | 向导稿 02 · 讨论记录 · 验收勾选 |

---

## 5. 给执行者的提醒

- 先 `--dry-run` 确认索引可解析，再正式跑 gbx。
- 章节结束配置属于 UI mock：写回节点 data 即可，禁止落盘与 Host save。
- 枚举的 UI 选项来源要收口到 typeFiles（例如等价 `callCardLabels.ts`），不要在组件内硬编码“枚举原文 + 中文”散落。

