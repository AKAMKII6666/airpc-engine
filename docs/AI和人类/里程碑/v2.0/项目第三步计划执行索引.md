---
# Studio V2.0 · 项目第三步计划执行索引（gbx / general-batch-exe）
# 用法：node .cursor/skills/general-batch-exe/bin/gbx.js --exFile <本文件> --workdir <仓库根> [--dry-run]
# 本轮目标：真实业务接线（非 mock 模拟）；接通后立刻删对应 mock；第一幕 wrong_number_act1；编辑器读写+接线
batch_size: 1
max_rounds: 80
max_fix_attempts: 5
max_ineffective_fixes: 2
stop_on_fail: true
group: order

verify_default:
  - npm run quality:studio

read_first:
  - AGENTS.md
  - .cursor/rules/codingRole.mdc
  - .cursor/rules/studio-boundaries.mdc
  - .cursor/rules/studio-v2-quality.mdc
  - .cursor/rules/testing-and-quality.mdc
  - .cursor/rules/docs-execution.mdc
  - docs/AI和人类/里程碑/v2.0/第三步需求.md
  - docs/AI/00-Studio-V2代码事实索引.md
  - docs/AI/studio-v2/02-角色库与人设编辑.md
  - docs/AI/studio-v2/04-第一幕打错电话-mock拓扑.md
  - docs/AI/studio-v2/07-已知边界与下一步候选.md
  - docs/AI和人类/需求/01-CallCard数据模型.md
  - docs/AI和人类/需求/11-角色与社交.md
  - docs/AI和人类/需求/10-用户与存档.md
  - apps/studioV2/src/utils/ajaxProxy/library/api/charactersApi.ts
  - apps/studioV2/src/utils/ajaxProxy/library/api/usersApi.ts
  - apps/studioV2/src/bis/pageBis/storyEditor/package/diskBundleGraph.ts
  - apps/studioV2/src/bis/pageBis/storyEditor/package/loadStoryPackage_bis.ts
  - apps/studioV2/src/bis/pageBis/storyEditor/package/saveStoryPackage_bis.ts
  - data/storis-packages/golden_handoff/story.conf.json
  - data/storis-packages/golden_handoff/canvas.layout.json

hard_stop_patterns:
  - "协议未定|需要人工决策|阻塞实现"
  - "跳过阶段 A 直接做编辑接线|B 未通就做 C"
  - "继续以 MOCK_CHARACTERS 为角色库列表真源|角色增删改只写会话 mock"
  - "接通后保留角色 mock 备用|双轨真源\\+mock|不删 MOCK_CHARACTERS"
  - "故事编辑器仍以 CanvasMockGraph 为打开真源|打开包不读盘"
  - "保存只改内存不写 data/storis-packages|假装已保存"
  - "列表仍用 MOCK_STORY_PACKAGES 当真源|mockWorkbench 作打开真源"
  - "packageId 仍用 pkg_memory_bar_1 作正式第一幕包"
  - "改 packages/rpg-engine 大行为|Effect 真执行|beginCall 闭环"
  - "深化 /debugger 真会话|整模块重做调试器"
  - "资源库强制落盘|导入导出正式管道作为本批硬依赖"
  - "单卡增量 API 替代整包保存且无整包路径"
  - "留下 @deprecated 已迁至|兼容转发壳|无引用 mock 文件不删"

executor_extra: |
  只实现当前 ACTIVE / 本轮选出的 ⬜ 行；完成后把对应行改为 ✅。
  需求真源：第三步需求.md（尤其 §2.0 真源+删 mock 零残余）。刀序 V2-T3-1 → V2-T3-8；严格 group: order。
  阶段：A 落盘（1–2）→ B 管道+清 mock（3–6）→ C 接线（7）→ 收口（8）。禁止跳步。

  真源铁律：
  - 做真实业务接线，不是 mock 模拟。
  - 某域接通并验收后，同一任务内删除该域 mock 源与业务引用；禁止「先接上、mock 留着」。
  - 本步必须清零：角色库、玩家列表/CRUD、故事包列表、编辑器打开/保存种子。
  - 允许暂留：资源库 mock、调试器 mock、设置页纯展示 mock。
  - 禁止双轨开关、re-export 兼容壳、无引用文件残留。

  内容：
  - 五人 agentId：lanxing / xiaopi / qiang-shushu / zhang-boss / bai-bansian；personalityCode 与 docs/AI 一致。
  - 故事包 packageId=wrong_number_act1；入口卡 lanxing_wrong_number；三张卡对齐 docs/AI 拓扑蓝本。
  - layout 与 conf/cards 职责分离；整包保存。

  管道：
  - 角色/玩家走 /api/* → data/。
  - 故事包 BFF 列/读/写；列表与编辑器打开真源仅为磁盘。

reviewer_extra: |
  只读审查；禁止改业务代码。写出带本轮 reviewRunId 的 reviews/latest.json。
  对照 第三步需求.md §1–§3 与 §2.0。
  blocker/critical：仍 mock 当真源、接通后 mock 未删、双轨/兼容壳、打不开 wrong_number_act1、
  保存不落盘、跳过 A/B 做 C、packageId 用内存条旧名、quality:studio 失败、未更新 docs/AI。

fixer_extra: |
  只根据审查报告与 latest-verify.json 修确认成立的问题；禁止顺手重构无关模块。
  不得用删注释、压行、大范围 eslint-disable、门禁 allowlist 掩盖结构问题。
  触碰历史空格缩进文件须整文件改 tab（STUDIO-STRUCT-017）。
  清 mock 时勿误删资源库/调试器允许暂留的文件。

workflow_dir: .ai-workflow-v2-step3
adapter: table
---

# Studio V2.0 · 项目第三步计划执行索引

> **用法：**  
> `node .cursor/skills/general-batch-exe/bin/gbx.js --exFile docs/AI和人类/里程碑/v2.0/项目第三步计划执行索引.md --workdir . --dry-run`  
> 通过后再去掉 `--dry-run` 正式跑。  
> **独立 workflow：** `.ai-workflow-v2-step3/`（勿与 refine7 / 第二步 workflow 混用）。  
> **前置：** 第二步交互深化已收口；细化修改 4–7 已站稳；`docs/AI` 已固化拓扑蓝本。

本文回答：**真源接通与删 mock 按什么顺序做、做到哪算完**。  
需求真源：**[第三步需求.md](./第三步需求.md)**（含 **§2.0 零残余**）。

---

## 0. 已钉死口径

### 0.1 范围边界

| 做 | 不做（本轮） |
|----|----------------|
| 五人角色 JSON 落盘 | 引擎大改 / Effect 真执行 |
| `wrong_number_act1` 真包落盘 | 第二幕内容 |
| 角色/玩家/故事/编辑器真源 | 资源库强制落盘 |
| 接通后**立刻删**对应 mock | 调试器/资源 mock 暂留 |
| 编辑器读盘 + 整包保存 + 编辑接线 | 单卡增量 API 唯一路径；mock 双轨备用 |

### 0.2 推荐刀序

```text
V2-T3-1  五人 CharacterDef 落 data/characters
V2-T3-2  wrong_number_act1 包落盘（conf + cards + layout）
V2-T3-3  角色 CRUD 接通 + 删除角色业务 mock（零残余）
V2-T3-4  玩家 CRUD 验收 + 删除玩家列表 mock 残余
V2-T3-5  故事包 BFF：列 / 读 / 写（整包）
V2-T3-6  列表与编辑器读写 + 删除包列表/打开路径 mock 真源
V2-T3-7  编辑接线落盘；清理编辑器仅 mock 的无引用种子
V2-T3-8  docs/AI 与验收勾选；确认清零范围无引用残余
```

`group: order`：严格按表从上到下取 ⬜。

> **gbx 表解析注意：** 任务单元格勿写英文 `status`、勿写「状态」子串；避免同行同时触发 `id` 子串与「任务」误判。

### 0.3 引擎/写盘声明（全批）

```text
行为目标：真实业务接线；角色/玩家/故事/编辑器真源；接通即删 mock；第一幕真实包可读写可编辑落盘
目标模块：data/characters、data/storis-packages、apps/studioV2 app/api 与 page/bis/ajaxProxy
允许修改：上述 + tests/** + docs/AI/** + 本批里程碑勾选；删除本步清零范围内的 mock 文件
禁止落点：Host 闭环、debugger 深化、mock 双轨、以 CanvasMockGraph/MOCK_* 为业务真源、无引用兼容壳
状态不变量：Content 在 data/；保存后刷新可见；整包保存；清零域无业务 mock 入口
verify：npm run quality:studio
```

---

## 1. 任务表

| 状态 | ID | 任务 | verify |
|------|-----|------|--------|
| ✅ | V2-T3-1 | **角色落盘**：正式五人写入 `data/characters/*.json`（agentId 与 personalityCode 对齐 docs/AI）；可与 golden 样例并存 | npm run quality:studio |
| ✅ | V2-T3-2 | **第一幕真包**：新建 `data/storis-packages/wrong_number_act1/`（story.conf + 三张 s-card + canvas.layout）；叙事对齐 docs/AI 拓扑蓝本；packageId 禁用内存条旧名 | npm run quality:studio |
| ✅ | V2-T3-3 | **角色 CRUD + 删 mock**：列表/新建/删除/详情与编辑器角色弹窗全部 `/api/characters`；**同一任务内**删除 MOCK_CHARACTERS/mockCharactersData/listMockCharacters/commitUpdateCharacterMock 等业务入口与无引用文件 | npm run quality:studio |
| ✅ | V2-T3-4 | **玩家 CRUD + 删 mock**：验收 `/api/users` 增删改刷新仍在；**同一任务内**删除 listMockUserProfiles/MOCK_USER_PROFILES 作列表或增删真源的残余 | npm run quality:studio |
| ✅ | V2-T3-5 | **故事包 BFF**：列包/读包/整包写回 `data/storis-packages`；与 golden_handoff 格式兼容；server 落点遵守 V2 目录规则 | npm run quality:studio |
| ✅ | V2-T3-6 | **列表与编辑器读写 + 删 mock**：列表与打开均读盘；保存写盘；**同一任务内**去除 MOCK_STORY_PACKAGES/mockWorkbench/CanvasMockGraph 作为列表或打开真源；无引用种子删除或仅留测试夹具 | npm run quality:studio |
| ✅ | V2-T3-7 | **编辑接线**：浮窗/出口/Effect/归属可保存落盘；锚点对齐磁盘角色；清理编辑器仅 mock 且已无引用的合并/种子函数 | npm run quality:studio |
| ✅ | V2-T3-8 | **收口**：需求验收勾选；更新 docs/AI 为真源+已删 mock；抽查清零范围无业务引用残余 | npm run quality:studio |

---

## 2. 批次出口（每批结束后）

- 本批任务行均为 ✅
- 任务 `verify` / `verify_default` exit 0
- Reviewer：`{workflow_dir}/reviews/latest.json` 中本轮 `blocker=0` 且 `critical=0`

额外关注：

- **T3-1 / T3-2**：磁盘内容是否齐  
- **T3-3**：刷新后角色仍在；**角色 mock 文件/引用是否已删**  
- **T3-4**：玩家 mock 列表入口是否已删  
- **T3-5**：整包写盘；缺 layout 安全回落  
- **T3-6**：重进不丢改；**打开路径是否已无 CanvasMockGraph/MOCK 包列表真源**  
- **T3-7**：浮窗改动能落盘 cards  
- **T3-8**：docs/AI 与清零范围一致  

---

## 3. 本轮总出口（全部 ✅ 后）

- [x] 五人角色落盘且库页 CRUD 真源；角色业务 mock 已删  
- [x] 玩家 CRUD 真源；玩家列表 mock 已删  
- [x] `wrong_number_act1` 落盘；列表来自磁盘  
- [x] 编辑器打开真包、保存回盘；打开/列表 mock 真源已删  
- [x] 浮窗/出口/Effect 编辑可落盘  
- [x] 清零范围内无双轨/兼容壳/无引用 mock 业务入口  
- [x] `npm run quality:studio` 全绿  
- [x] `docs/AI` 已更新  

收口后 **不要在本文件继续加任务**。

---

## 4. 任务 ↔ 需求映射

| 任务 | 第三步需求 |
|------|------------|
| V2-T3-1 | §2.1 · 阶段 A1 |
| V2-T3-2 | §2.3 · 阶段 A2 |
| V2-T3-3 | §2.0 · §2.1 · 阶段 B1 |
| V2-T3-4 | §2.0 · §2.2 · 阶段 B2 |
| V2-T3-5 | §2.4 · 阶段 B3 |
| V2-T3-6 | §2.0 · §2.5 B · 阶段 B4 |
| V2-T3-7 | §2.5 C · 阶段 C |
| V2-T3-8 | §3 验收 · docs/AI |

---

## 5. 给执行者的提醒

- **先 `--dry-run`** 再正式跑 gbx。  
- **接通即删 mock**（需求 §2.0）；资源/调试器 mock 勿误删。  
- 落盘蓝本用 docs/AI 拓扑，运行时打开只用磁盘。  
- 整包保存；layout 与 cards 分离（需求 §2.6）。  
- 脏树 + `--checkpoint` 会 BLOCKED；需要时 `--clear-blocked`。  
- studio-v2 门禁：tab、入口解构注释、无 barrel、触碰即改缩进。
