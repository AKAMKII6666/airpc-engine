---
# Studio V2.0 · 细化修改 1 执行索引（gbx / general-batch-exe）
# 用法：node .cursor/skills/general-batch-exe/bin/gbx.js --exFile <本文件> --workdir <仓库根> [--dry-run]
# 本轮目标：引擎去时段桶 + identity 补字段 → AutoForm 升级 → /characters 重编排 → 其它表单对齐。

batch_size: 3
max_rounds: 80
max_fix_attempts: 5
max_ineffective_fixes: 2
stop_on_fail: true
group: order

verify_default:
  - npm run quality:studio

read_first:
  - AGENTS.md
  - .cursor/rules/codingRole.mdc
  - .cursor/rules/engine-boundaries.mdc
  - .cursor/rules/engine-code-quality.mdc
  - .cursor/rules/studio-v2-quality.mdc
  - docs/AI和人类/里程碑/v2.0/细化修改1_需求.md
  - docs/AI和人类/需求/11-角色与社交.md
  - docs/AI和人类/需求/01-CallCard数据模型.md
  - docs/AI和人类/sudioV2.0版本/需求/ui/09-Studio-V2前端目录结构与落点规则.md
  - docs/AI和人类/sudioV2.0版本/需求/ui/08-Studio-V2前端工程规则与质量门禁需求.md

hard_stop_patterns:
  - "协议未定|需要人工决策|阻塞实现"
  - "保留 timeBuckets|兼容时段桶|静默忽略 timeBuckets"
  - "记忆写入|编辑 Memory|把记忆写入角色"
  - "音色枚举进引擎|Realtime 真链|电话壳"
  - "调试器改版|DebuggerShell 深化"
  - "引入 Mithril 依赖|照搬 Mithril 样式|jquery"
  - "社交边完整 UI|Free 卡深编辑器"

executor_extra: |
  只实现当前 ACTIVE / 本轮选出的 ⬜ 行；完成后把对应行改为 ✅。
  需求真源：细化修改1_需求.md。刀序必须 R1 → R2 → R3 → R4，不得跳过引擎契约做角色落盘。
  引擎批（R1-*）：verify 用 npm run quality:engine；遵守 §0.4 高风险落点声明。
  Studio 批（R2/R3/R4）：verify 用 npm run quality:studio（或任务列指定命令）。
  AutoForm：借鉴 Mithril 编排方法（items + ComsMap + 自动绑 + comProps/children 双逃生口）；样式用 Studio V2 MUI+scss；禁止引入 Mithril 包。
  旧 FormSchemaRenderer/kind：可短暂 adapter，本轮收口前主路径须切到 AutoForm。
  角色编辑态：界面展示字段全必填；timeBuckets 禁止出现在表单与 JSON。
  记忆区只读；禁止写角色 JSON。
  不要改本索引 frontmatter 语义与「已钉死口径」；不要顺手扩社交边完整 UI / 调试器改版。

reviewer_extra: |
  只读审查；禁止改业务代码。写出带本轮 reviewRunId 的 reviews/latest.json（见 general-batch-exe/docs/02-review-json.md）。
  对照 细化修改1_需求.md §3–§8。
  blocker/critical：残留 timeBuckets、无 localHourRange 匹配、AutoForm 缺少自动绑或双逃生口、角色表单与 CharacterDef 脱节、记忆可写、引擎批未跑 quality:engine、无豁免的 eslint-disable、门禁失败。
  R1 未收口不得放行 R3「已保存角色 JSON」假完成。

fixer_extra: |
  只根据审查报告与 latest-verify.json 修确认成立的问题；禁止顺手重构无关模块。
  不得用删注释、压行、大范围 eslint-disable、门禁 allowlist 掩盖结构问题。
  引擎修复须保持 quality:engine 结构预算不恶化。

workflow_dir: .ai-workflow-v2-refine1
adapter: table
---

# Studio V2.0 · 细化修改 1 执行索引

> **用法：**  
> `node .cursor/skills/general-batch-exe/bin/gbx.js --exFile docs/AI和人类/里程碑/v2.0/细化修改1_执行索引.md --workdir . --dry-run`  
> 通过后再去掉 `--dry-run` 正式跑。  
> **独立 workflow：** `.ai-workflow-v2-refine1/`（勿与 step1/step2 workflow 混用）。

本文回答：**细化修改 1 按什么顺序做、做到哪算完**。  
需求真源：**[细化修改1_需求.md](./细化修改1_需求.md)**。  
人类讨论：[原始需求讨论记录](../../../人类/原始需求讨论记录)（约 612–707 行）。

---

## 0. 已钉死口径

### 0.1 范围边界

| 做 | 不做（本轮） |
|----|----------------|
| 删除 `timeBuckets` / `TimeBucket`；只留 `localHourRange` | 时段桶兼容层、静默忽略旧字段 |
| `identity.fullName` / `nickname` | — |
| AutoForm：`items[]` + ComsMap + 自动绑 + `comProps`/`children` 逃生口 | 引入 Mithril / jquery |
| `/characters` 按需求 §4 重编排；角色 JSON 可经 API 写盘 | 社交边完整 UI、Free 卡深编辑 |
| 记忆只读列表 + 分页 | 记忆写入 |
| users / assets / FormModal 对齐 AutoForm | 调试器改版、电话壳 Realtime 真链 |

### 0.2 旧内容策略

含 `timeBuckets` 的内容包 / 角色卡：**拒载**。仓内样例本轮改完。

### 0.3 推荐刀序

```text
R1-*  引擎契约
R2-*  AutoForm 基建与控件
R3-*  角色库重编排 + 记忆只读
R4-*  其它表单对齐
```

`group: order`：严格按表中从上到下取 ⬜；每轮最多 `batch_size` 条。

> **gbx 表解析注意：** 任务单元格勿写英文 `status`、勿写「状态」子串；勿在同一行同时出现易触发表头误判的 `id` 子串与「任务」二字（本表已规避）。

### 0.4 高风险引擎批落点声明（R1-*）

```text
行为目标：promptScenes 匹配仅 localHourRange；去掉 TimeBucket；identity 增加 fullName/nickname；旧桶字段拒载
目标模块：packages/rpg-engine/src/schema/promptScene.ts、schema/character.ts、runtime/composer.ts、runtime/composeScene.ts、host/types.ts、validation（若有）、index 门面
允许修改：上述模块 + packages/rpg-engine/tests/** + data/** 样例 JSON + docs/AI和人类/需求/01、11、50（及必要交叉链接）
禁止落点：不得把匹配逻辑堆回 createEngineHost；不得在 effectExecutor / scheduleTick 塞入时段逻辑；不得保留 timeBuckets 兼容分支
状态不变量：layerMatches 对小时区间半开；isEffectiveDialable 语义不变；引擎仍无 React/DOM
回归测试：Composer 仅 localHourRange 正/负向；含 timeBuckets 的夹具拒载或校验失败；hourToTimeBucket 删除后无残留引用
verify：npm run quality:engine
结构预算：触碰基线文件时受控指标不得净增长
```

---

## 1. 任务表

| 状态 | ID | 任务 | verify |
|------|-----|------|--------|
| ✅ | V2-R1-1 | **去时段桶（schema）**：删除 TimeBucket 相关 schema/导出；`match` 只保留 localHourRange；ComposeScene.localTime 去掉 bucket | npm run quality:engine |
| ✅ | V2-R1-2 | **去时段桶（运行时+测试）**：composer/composeScene 只按 localHourRange 匹配；重写 composer 相关测试；删除 hourToTimeBucket | npm run quality:engine |
| ✅ | V2-R1-3 | **去时段桶（样例+需求文）**：改仓内含 timeBuckets 的 JSON；同步需求 01/11/50；加载含桶字段则拒载（可测） | npm run quality:engine |
| ✅ | V2-R1-4 | **identity 补字段**：CharacterDef 增加 fullName/nickname；更新需求 11；样例可选填 | npm run quality:engine |
| ✅ | V2-R2-1 | **AutoForm 骨架**：在 commonUi 落地 AutoForm（formik/mode/enabled/items）；ComsMap；按 name 自动绑（含嵌套路径）；保留 comProps 与 children 双逃生口 | npm run quality:studio |
| ✅ | V2-R2-2 | **基础控件迁入 ComsMap**：TextField/Select/Checkbox/AutoTextArea（自适应增高）/IntegerInput/DateField；字段壳 label/必填星/错误/watch | npm run quality:studio |
| ✅ | V2-R2-3 | **FormModal 切 AutoForm**：FormModal 默认走 AutoForm；旧 kind 配置可短暂 adapter，主路径不再依赖 FormSchemaRenderer | npm run quality:studio |
| ✅ | V2-R2-4 | **角色专用块控件**：AvatarUpload、StringListEditor、PromptVariantListEditor、LocalHourRangeField、PromptSceneListEditor（增删/折叠/拖拽写 priority） | npm run quality:studio |
| ✅ | V2-R3-1 | **角色详情 AutoForm 编排**：按需求 §4 用 items[] 重做详情；编辑态展示字段全必填；去掉与引擎脱节的扁平 mock 字段 | npm run quality:studio |
| ✅ | V2-R3-2 | **角色 JSON 读写**：经 Next API 门面读/写 data/characters；保存无 timeBuckets；全名/昵称/话术列表/场景卡可回读 | npm run quality:studio |
| ✅ | V2-R3-3 | **记忆只读区**：按当前 userId+agentId 查 Memory；列表+分页；无写口、不进角色 JSON | npm run quality:studio |
| ✅ | V2-R4-1 | **对齐创建弹窗**：角色/资源/用户等 FormModal 创建表改 AutoForm items[] | npm run quality:studio |
| ✅ | V2-R4-2 | **对齐 users/assets 详情**：UserDetail/AssetDetail 改 AutoForm items[]；删双轨 kind 主路径 | npm run quality:studio |
| ✅ | V2-R4-3 | **收口清理**：删除或明确废弃旧 FormSchemaRenderer 主路径；更新 09/导向稿必要段落；确认 quality:studio 全绿 | npm run quality:studio |

---

## 2. 批次出口（每批结束后）

单批「完成」须同时满足：

- 本批任务行均为 ✅
- 任务 `verify` / `verify_default` 命令 exit 0
- Reviewer：`{workflow_dir}/reviews/latest.json` 中本轮 `blocker=0` 且 `critical=0`

额外人工关注（Reviewer 摘要中写明即可）：

- **R1**：是否仍导出 TimeBucket；样例是否还有 timeBuckets；拒载是否可测
- **R2**：自动绑是否覆盖嵌套 name；comProps/children 逃生口是否可用；AutoTextArea 是否自适应
- **R3**：编辑态是否全必填；落盘是否对齐 CharacterDef；记忆是否只读
- **R4**：是否仍残留 kind 主路径双轨

---

## 3. 本轮总出口（全部 ✅ 后）

- [ ] [细化修改1_需求.md](./细化修改1_需求.md) §8 总验收项可勾选
- [ ] `npm run quality:engine` 全绿（R1 相关）
- [ ] `npm run quality:studio` 全绿
- [ ] 仓内无 `timeBuckets`；角色详情无时段桶 UI
- [ ] AutoForm 为表单主编排；双逃生口保留
- [ ] 未做社交边完整 UI / 记忆写入 / 调试器改版 / Mithril 依赖

收口后 **不要在本文件继续加任务**；后续里程碑另开索引。

---

## 4. 任务 ↔ 需求映射

| 任务组 | 细化修改1_需求 | 其它 |
|--------|----------------|------|
| V2-R1-* | §3.1–3.2 · §5.1 | 需求 01/11/50 · quality:engine |
| V2-R2-* | §3.7 · §5.2 | Mithril AutoForm 方法参考 · 08/09 |
| V2-R3-* | §3.3–3.6 · §4 · §5.3 | 导向稿 05 · 需求 11/12 |
| V2-R4-* | §5.4 | 第二步已有 users/assets 页 |

---

## 5. 给执行者的提醒

- **先 R1 后 R3 落盘**：没有去掉 timeBuckets、没有 fullName/nickname，不要宣称角色 JSON 已对齐。
- **先 R2 后重编排**：没有 AutoForm 不要在角色页继续堆平行 FormSchemaRenderer 字段数组。
- 场景匹配 UI **只做** localHourRange；不要做时段桶多选。
- 过程话术 / 样例句用 **列表编辑器**，不要单框假装数组。
- 需要 git checkpoint 时显式：`gbx ... --checkpoint`（脏树会 BLOCKED）。
