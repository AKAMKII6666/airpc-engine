---
# Studio V2.0 · 细化修改 6 执行索引（gbx / general-batch-exe）
# 用法：node .cursor/skills/general-batch-exe/bin/gbx.js --exFile <本文件> --workdir <仓库根> [--dry-run]
# 本轮目标：底栏 10 icon mock 可用 · toolMode · 放置创建 · fitView/框选/连线
batch_size: 2
max_rounds: 60
max_fix_attempts: 5
max_ineffective_fixes: 2
stop_on_fail: true
group: order

verify_default:
  - npm run quality:studio

read_first:
  - AGENTS.md
  - .cursor/rules/codingRole.mdc
  - .cursor/rules/studio-boundaries.mdc
  - .cursor/rules/studio-v2-quality.mdc
  - .cursor/rules/testing-and-quality.mdc
  - docs/AI和人类/里程碑/v2.0/细化修改4_需求.md
  - docs/AI和人类/里程碑/v2.0/细化修改5_需求.md
  - docs/AI和人类/里程碑/v2.0/细化修改6_需求.md
  - docs/AI和人类/sudioV2.0版本/需求/ui/02-新版故事编辑器界面UI设计向导稿.md
  - apps/studioV2/src/pageComponents/storyEditor/BottomToolBar.tsx
  - apps/studioV2/src/pageComponents/storyEditor/canvas/storyCanvasTypes.ts

hard_stop_patterns:
  - "协议未定|需要人工决策|阻塞实现"
  - "恢复 CharacterAnchorRail|恢复左侧角色轨|恢复底栏角色浮窗|恢复 CharacterPickerFloat"
  - "通话卡/包/画布写盘|写入 storis-packages|story\\.conf\\.json 写盘|Host save|EngineHost 写口"
  - "底栏工具仍全部 disabled|tooltip.*界面预览未开放|保留界面预览未开放"
  - "让用户手填 cardId|让用户手填 nodeId|创建主路径.*TextField.*cardId"
  - "把 action 或 comment 塞进 EditorCallCardProjection|把动作节点写进 CallCardDefinition"
  - "改 packages/rpg-engine|CallCardDefinitionSchema 变更|引擎 schema 修改"
  - "调试器改版|修改 DebuggerShell"
  - "在 BottomToolBar 内直接 mutate React Flow|不经 bis 直接改 nodes 状态"

executor_extra: |
  只实现当前 ACTIVE / 本轮选出的 ⬜ 行；完成后把对应行改为 ✅。
  需求真源：细化修改6_需求.md。刀序 S6-1 → S6-6；严格 group: order。

  底栏与画布：
  - 10 个原 disabled icon 必须可点；去掉「界面预览未开放」。
  - toolMode 模式编排放 bis/pageBis/storyEditor/dock/；StoryCanvasStageApi 扩展 setToolMode/fitView/addNodeAt。
  - 放置流程：底栏选类型 → onPaneClick → screenToFlowPosition → 节点工厂 → 选中 + 属性浮窗。
  - cardId/nodeId 系统生成；禁止手填为主路径。

  节点：
  - CallCard 五种 + chapter_end 复用现有节点类型与属性窗。
  - action / commentGroup 为独立 type + 轻量 data；不进引擎 schema。

  范围：
  - 会话 mock only；不写 storis-packages；不改引擎。
  - 资源/包配置浮窗保持 refine4 行为。

reviewer_extra: |
  只读审查；禁止改业务代码。写出带本轮 reviewRunId 的 reviews/latest.json（见 general-batch-exe/docs/02-review-json.md）。
  对照 细化修改6_需求.md §2–§5。
  blocker/critical：底栏仍 disabled、无 placement/fitView/模式切换、放置后不选中不开浮窗、手填 ID、写盘/引擎改动、action 混入 CallCard、quality:studio 失败。

fixer_extra: |
  只根据审查报告与 latest-verify.json 修确认成立的问题；禁止顺手重构无关模块。
  不得用删注释、压行、大范围 eslint-disable、门禁 allowlist 掩盖结构问题。

workflow_dir: .ai-workflow-v2-refine6
adapter: table
---

# Studio V2.0 · 细化修改 6 执行索引

> **用法：**  
> `node .cursor/skills/general-batch-exe/bin/gbx.js --exFile docs/AI和人类/里程碑/v2.0/细化修改6_执行索引.md --workdir . --dry-run`  
> 通过后再去掉 `--dry-run` 正式跑。  
> **独立 workflow：** `.ai-workflow-v2-refine6/`（勿与 refine4/5 混用）。  
> **前置：** 细化修改 4、5 任务已 ✅。

本文回答：**故事编辑器底栏工具 mock 化按什么顺序做、做到哪算完**。  
需求真源：**[细化修改6_需求.md](./细化修改6_需求.md)**。  
人类讨论：[原始需求讨论记录](../../../人类/原始需求讨论记录)（底栏 icon 需全部可点）。

---

## 0. 已钉死口径

### 0.1 范围边界

| 做 | 不做（本轮） |
|----|----------------|
| 底栏 10 icon 可点 + active 高亮 | 恢复角色轨 / 底栏角色浮窗 |
| toolMode + fitView / 框选 / 连线 | 出口拖空白创建菜单（§7.2） |
| 五种 CallCard + chapter_end 放置 | 写盘 storis-packages / Host |
| action / comment 轻量节点 | 引擎 schema 变更 |
| 资源 + 包配置浮窗保持 | 调试器改版 |

### 0.2 与 refine4/5 的依赖

- 必须已有 CallCard / 章节属性浮窗、资源与包配置浮窗。
- chapter_end 放置后须能走 refine5 的 `nextPackageId` / `nextEntryCardId` 配置。
- **不** 重做 refine5 Effect / toolPolicy 枚举控件。

### 0.3 推荐刀序

```text
S6-1  toolMode 模式编排 + BottomToolBar 接线（去掉 disabled）
S6-2  fitView + 框选 / 连线模式互斥
S6-3  CallCard 放置工厂（story / playback / free / schedule）
S6-4  chapter_end 放置 + 放置后选中开浮窗
S6-5  action / commentGroup 轻量节点
S6-6  文档同步 + 测试 + 验收勾选
```

`group: order`：严格按表从上到下取 ⬜。

> **gbx 表解析注意：** 任务单元格勿写英文 `status`、勿写「状态」子串；避免同行同时触发 `id` 子串与「任务」误判。

### 0.4 引擎/写盘声明（全批）

```text
行为目标：Studio V2 故事编辑器底栏工具 mock；放置创建仅会话内 nodes/edges
目标模块：apps/studioV2/src/pageComponents/storyEditor/**、bis/pageBis/storyEditor/dock/**、typeFiles/story/**
允许修改：上述 + tests/storyEditor/** + docs（本批列明）
禁止落点：packages/rpg-engine/**、data/storis-packages 写盘、Host/API 新 save 端点
状态不变量：新节点仅内存；资源/角色可走已有 API；CallCard 属性「应用到画布」= 会话内
verify：npm run quality:studio
```

---

## 1. 任务表

| 状态 | ID | 任务 | verify |
|------|-----|------|--------|
| ✅ | V2-S6-1 | **toolMode + 底栏接线**：`dockToolMode` 模式编排；BottomToolBar 接 activeTool/onToolClick；去掉 10 icon disabled 与「界面预览未开放」；资源/包 icon 保持 | npm run quality:studio |
| ✅ | V2-S6-2 | **视图工具**：StoryCanvasStageApi 增 fitView/setToolMode；fit 瞬时适配；框选(selectionOnDrag)与连线(connect)模式互斥；壳层与底栏高亮同步 | npm run quality:studio |
| ✅ | V2-S6-3 | **CallCard 放置**：节点工厂 + onPaneClick 放置；支持 story/playback/free/schedule 四种默认投影；系统生成 cardId/nodeId；放置后选中并开属性浮窗 | npm run quality:studio |
| ✅ | V2-S6-4 | **chapter_end 放置**：底栏章节结束 icon 进入 placement；生成 chapter_end 节点；选中打开章节属性浮窗（含 refine5 字段） | npm run quality:studio |
| ✅ | V2-S6-5 | **action / comment 轻量节点**：新增 ActionFlowNode、CommentGroupFlowNode；独立 data 类型；可放置拖动选中；不进 CallCard 投影 | npm run quality:studio |
| ✅ | V2-S6-6 | **文档同步**：细化修改6 验收勾选；向导稿 §3.4/§7.1；讨论记录追加 refine6 结论 | — |

---

## 2. 批次出口（每批结束后）

- 本批任务行均为 ✅
- 任务 `verify` / `verify_default` exit 0
- Reviewer：`{workflow_dir}/reviews/latest.json` 中本轮 `blocker=0` 且 `critical=0`

额外关注：

- **S6-1**：是否仍 `enabled: false` 或 tooltip 含「界面预览未开放」
- **S6-2**：fitView 是否 no-op；框选与连线是否无法切换
- **S6-3**：放置是否仍要求手填 cardId；是否未 screenToFlowPosition
- **S6-4**：chapter_end 是否误走 CallCard 属性窗
- **S6-5**：action 是否混入 EditorCallCardProjection

---

## 3. 本轮总出口（全部 ✅ 后）

- [ ] 底栏 10 工具 icon 可点且有 active 反馈
- [ ] fitView / 框选 / 连线可用
- [ ] 五种 CallCard + chapter_end 可放置并自动开属性浮窗
- [ ] action / comment 轻量节点可放置
- [ ] 资源 / 包配置浮窗仍正常
- [ ] `npm run quality:studio` 全绿
- [ ] 未写 storis-packages；未改引擎

收口后 **不要在本文件继续加任务**。

---

## 4. 任务 ↔ 需求映射

| 任务组 | 细化修改6_需求 | 其它 |
|--------|----------------|------|
| V2-S6-1 · S6-2 | §2.1 · §2.2 · §2.5 | BottomToolBar · StoryCanvasStage |
| V2-S6-3 · S6-4 | §2.3 · §2.3 默认投影 | createStudioId · FloatingPanelShell |
| V2-S6-5 | §2.4 | 新 node type |
| V2-S6-6 | §4 · §5 | 向导稿 02 · 讨论记录 |

---

## 5. 给执行者的提醒

- **先 `--dry-run`** 确认索引可解析，再正式跑 gbx。
- 放置一次后回 idle（第一版）；Esc 取消 placement。
- 打开资源/包浮窗时取消 placement，避免误触画布。
- 节点工厂放 `bis/pageBis/storyEditor/dock/`，勿堆在 BottomToolBar.tsx。
- 需要 git checkpoint：`gbx ... --checkpoint`（脏树会 BLOCKED）。
