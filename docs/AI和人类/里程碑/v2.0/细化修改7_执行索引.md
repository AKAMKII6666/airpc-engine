---
# Studio V2.0 · 细化修改 7 执行索引（gbx / general-batch-exe）
# 用法：node .cursor/skills/general-batch-exe/bin/gbx.js --exFile <本文件> --workdir <仓库根> [--dry-run]
# 本轮目标：出口 Effect 列表升级为「每种 effect 专属参数面板」· tooltip · attach/unmount 连线同步
batch_size: 2
max_rounds: 60
max_fix_attempts: 5
max_ineffective_fixes: 2
stop_on_fail: true
group: order

verify_default:
  - npm run quality:studio

read_first:
  - AGENTS.md
  - .cursor/rules/codingRole.mdc
  - .cursor/rules/studio-boundaries.mdc
  - .cursor/rules/studio-v2-quality.mdc
  - .cursor/rules/testing-and-quality.mdc
  - docs/AI和人类/里程碑/v2.0/细化修改5_需求.md
  - docs/AI和人类/里程碑/v2.0/细化修改6_需求.md
  - docs/AI和人类/里程碑/v2.0/细化修改7_需求.md
  - docs/AI和人类/sudioV2.0版本/需求/ui/02-新版故事编辑器界面UI设计向导稿.md
  - packages/rpg-engine/src/runtime/effectExecutor.ts
  - packages/rpg-engine/src/schema/outcome.ts
  - apps/studioV2/typeFiles/story/editor/callCard/editorCallCardProjection.ts
  - apps/studioV2/typeFiles/story/callCardLabels.ts
  - apps/studioV2/src/pageComponents/storyEditor/com/panel/ExitListEditor/com/ExitEffectsMockList.tsx
  - apps/studioV2/src/pageComponents/storyEditor/com/panel/ExitListEditor/com/ExitListRow.tsx
  - apps/studioV2/src/bis/pageBis/storyEditor/form/exitList/exitListForm.ts
  - apps/studioV2/src/pageComponents/storyEditor/canvas/canvasNodeQueries.ts

hard_stop_patterns:
  - "协议未定|需要人工决策|阻塞实现"
  - "改 packages/rpg-engine|CallCardDefinitionSchema 变更|引擎 schema 修改|EffectSchema 修改"
  - "import 引擎运行时值当参数|从 rpg-engine 引 effectExecutor 值"
  - "写入 storis-packages|story\\.conf\\.json 写盘|Host save|EngineHost 写口"
  - "让用户手填 agentId|让用户手填 cardId|让用户手填 packageId|自由文本.*id.*主路径"
  - "把 effect 参数塞进 EditorCallCardProjection 顶层|塞进 CallCardDefinition"
  - "attach 只做列表不画边|不同步效果边|effects 与连线不同步"
  - "效果边与故事流转边无法区分|复用故事流转边样式承载 attach"
  - "在 ExitEffectsMockList 单文件堆 14 种面板|不拆 effect 子组件"
  - "恢复 CharacterAnchorRail|恢复底栏角色浮窗"

executor_extra: |
  只实现当前 ACTIVE / 本轮选出的 ⬜ 行；完成后把对应行改为 ✅。
  需求真源：细化修改7_需求.md。刀序 S7-1 → S7-6；严格 group: order。

  契约与分发：
  - EditorExitEffectProjection 加 params（按 effect 判别式联合）；effect 收紧为 KnownEffectName。
  - effect 参数字段严格对齐引擎 effectExecutor.ts 读取（§2.2 表）；只读镜像，禁 import 引擎值。
  - ExitEffectsMockList 只负责列表 + 分发；每 effect 面板拆到 com/effects/ 子组件。

  下拉数据源（禁自由文本手填 id）：
  - 角色 ← listCharacterAnchors；卡 ← 画布 CallCard 节点；包 ← refine5 包源；片段 ← 资源浮窗。

  attach/unmount：
  - modal 列表可配多条；每条 ↔ 画布效果边（data.kind=attach/unmount），与故事流转边视觉可区分。
  - 正向：列表增删改 → 建/删/改效果边；反向：画布拖/删效果边 → 写 effects 行。
  - agentId 默认取 target 卡归属；modal 可覆盖。

  tooltip / summary：
  - EFFECT_NAME_OPTIONS 每项加 description；Effect 下拉 MenuItem 挂 Tooltip。
  - summarizeEffect(params) 自动派生 summary；用户手改后不覆盖。

  范围：
  - 会话 mock only；不写 storis-packages；不改引擎；不做 ExitCondition 树、不做 effect 真执行。

reviewer_extra: |
  只读审查；禁止改业务代码。写出带本轮 reviewRunId 的 reviews/latest.json（见 general-batch-exe/docs/02-review-json.md）。
  对照 细化修改7_需求.md §2–§6。
  blocker/critical：仍是单摘要死框、无专属面板、id 自由文本手填、attach/unmount 不画边或与流转边不可区分、
  effect 参数塞进 CallCard 顶层、改引擎/写盘、ExitEffectsMockList 未拆子组件、quality:studio 失败。

fixer_extra: |
  只根据审查报告与 latest-verify.json 修确认成立的问题；禁止顺手重构无关模块。
  不得用删注释、压行、大范围 eslint-disable、门禁 allowlist 掩盖结构问题。

workflow_dir: .ai-workflow-v2-refine7
adapter: table
---

# Studio V2.0 · 细化修改 7 执行索引

> **用法：**  
> `node .cursor/skills/general-batch-exe/bin/gbx.js --exFile docs/AI和人类/里程碑/v2.0/细化修改7_执行索引.md --workdir . --dry-run`  
> 通过后再去掉 `--dry-run` 正式跑。  
> **独立 workflow：** `.ai-workflow-v2-refine7/`（勿与 refine4/5/6 混用）。  
> **前置：** 细化修改 4、5、6 任务已 ✅；框选/toolPolicy 微调已在会话内完成。

本文回答：**出口 Effect 面板升级按什么顺序做、做到哪算完**。  
需求真源：**[细化修改7_需求.md](./细化修改7_需求.md)**。  
人类讨论：[原始需求讨论记录](../../../人类/原始需求讨论记录)（Effects 列表要每种有面板 + tooltip；挂卡/卸卡 modal 列表且与连线同步）。

---

## 0. 已钉死口径

### 0.1 范围边界

| 做 | 不做（本轮） |
|----|----------------|
| Effect 契约加 params（判别式） | 引擎 schema / runtime 改动 |
| 每种 effect 专属参数面板 | 写盘 storis-packages / Host save |
| A 类角色/卡/包/片段下拉 | 自由文本手填 id |
| attach/unmount modal + 效果边同步 | 完整 ExitCondition 条件树编辑器 |
| effect 选项 tooltip + summary 自动派生 | effect 真执行 / 预览 |

### 0.2 与 refine4/5/6 的依赖

- 必须已有出口列表编辑器、CallCard 节点、角色锚点、资源与包配置浮窗。
- 复用 refine5 包/卡下拉数据源、`EFFECT_NAME_OPTIONS` 枚举。
- **不** 重做底栏 / 放置 / 框选（refine6 + 会话微调已收口）。

### 0.3 推荐刀序

```text
S7-1  契约扩展（params 判别式 + effect 收紧）+ effect 选项 tooltip
S7-2  B 类简单面板（schedule_call_card / create_research_commitment / update_user_profile /
      set_world_fact / create_voicemail / play_system_prompt）+ keep_card_pending 无参说明
S7-3  B 类复合面板（end_story 清场+下一章入口 / schedule_recurring_call 周几多选）
S7-4  A 类目标下拉面板（set_character_unlocked / set_redial_slot / patch_memory / update_npc_knowledge）
S7-5  attach_call_card：modal 列表 + attach 效果边双向同步
S7-6  unmount_call_card 复用效果边机制 + summary 自动派生 + 文档同步 + 测试 + 验收
```

`group: order`：严格按表从上到下取 ⬜。

> **gbx 表解析注意：** 任务单元格勿写英文 `status`、勿写「状态」子串；避免同行同时触发 `id` 子串与「任务」误判。

### 0.4 引擎/写盘声明（全批）

```text
行为目标：Studio V2 出口 Effect 参数面板；effects 与效果边仅会话内 nodes/edges/Formik
目标模块：apps/studioV2/src/pageComponents/storyEditor/**、bis/pageBis/storyEditor/**、typeFiles/story/**
允许修改：上述 + tests/storyEditor/** + docs（本批列明）
禁止落点：packages/rpg-engine/**、data/storis-packages 写盘、Host/API 新 save 端点
状态不变量：effect params 与效果边仅内存；字段名对齐引擎 EffectSchema 但只读镜像不 import 值
verify：npm run quality:studio
```

---

## 1. 任务表

| 状态 | ID | 任务 | verify |
|------|-----|------|--------|
| ⬜ | V2-S7-1 | **契约 + tooltip**：`EditorExitEffectProjection` 加 `params`（按 effect 判别式联合，字段对齐 §2.2）；`effect` 收紧为 `KnownEffectName`；`EFFECT_NAME_OPTIONS` 每项加 `description`；Effect 下拉 MenuItem 挂 Tooltip | npm run quality:studio |
| ⬜ | V2-S7-2 | **B 类简单面板**：schedule_call_card / create_research_commitment / update_user_profile / set_world_fact / create_voicemail / play_system_prompt 专属字段面板；keep_card_pending 无参说明；`ExitEffectsMockList` 改为按 effect 分发到 `com/effects/` 子组件 | npm run quality:studio |
| ⬜ | V2-S7-3 | **B 类复合面板**：end_story（reason + 清场折叠 + 下一章入口子面板）；schedule_recurring_call（卡引用二选一 + 时/分 + daily/weekly + 周几多选） | npm run quality:studio |
| ⬜ | V2-S7-4 | **A 类目标下拉**：set_character_unlocked / set_redial_slot / patch_memory / update_npc_knowledge；角色/卡下拉取画布节点；禁自由文本手填 id | npm run quality:studio |
| ⬜ | V2-S7-5 | **attach 连线同步**：attach_call_card modal 列表（可多条）+ attach 效果边渲染（区别于流转边）；列表增删改 ↔ 建/删/改边；画布拖/删边 ↔ 写 effects 行；agentId 默认取 target 卡归属 | npm run quality:studio |
| ⬜ | V2-S7-6 | **unmount + summary + 收口**：unmount_call_card 复用效果边机制（缺省=当前卡/角色）；`summarizeEffect` 自动派生 summary（手改不覆盖）；细化修改7 验收勾选；向导稿补充；讨论记录追加结论 | npm run quality:studio |

---

## 2. 批次出口（每批结束后）

- 本批任务行均为 ✅
- 任务 `verify` / `verify_default` exit 0
- Reviewer：`{workflow_dir}/reviews/latest.json` 中本轮 `blocker=0` 且 `critical=0`

额外关注：

- **S7-1**：params 是否真判别式（每变体只含该 effect 合法字段），还是 catchall 透传；tooltip 是否真挂上
- **S7-2**：是否仍单摘要死框；`ExitEffectsMockList` 是否仍单文件堆面板未拆
- **S7-3**：end_story next 子块字段是否齐；schedule_recurring weekly 周几是否可选
- **S7-4**：id 字段是否仍自由文本；下拉是否取画布节点
- **S7-5**：是否只做列表不画边；效果边是否与流转边可区分；反向同步是否缺失
- **S7-6**：unmount 缺省语义是否正确；summary 手改后是否被覆盖

---

## 3. 本轮总出口（全部 ✅ 后）

- [ ] Effect 下拉每项有 tooltip
- [ ] 每种 effect 有专属参数面板，非单摘要死框
- [ ] A 类目标字段全下拉，无自由文本手填 id
- [ ] attach/unmount modal 列表 + 效果边双向同步，且与流转边可区分
- [ ] keep_card_pending 无参说明；end_story 复合面板齐
- [ ] summary 自动派生且可覆盖
- [ ] `npm run quality:studio` 全绿
- [ ] 未写 storis-packages；未改引擎

收口后 **不要在本文件继续加任务**。

---

## 4. 任务 ↔ 需求映射

| 任务组 | 细化修改7_需求 | 其它 |
|--------|----------------|------|
| V2-S7-1 | §2.5 · §2.6 | callCardLabels · editorCallCardProjection |
| V2-S7-2 | §2.2（B 简单）· §2.8 | ExitEffectsMockList 分发 · com/effects |
| V2-S7-3 | §2.2（end_story / schedule_recurring） | effectExecutor 参数真源 |
| V2-S7-4 | §2.2（A 类）· §2.4 | canvasNodeQueries · roleConnection |
| V2-S7-5 | §2.3 | effect 边同步模块 · attach modal |
| V2-S7-6 | §2.3 · §2.7 · §4 · §5 | summarizeEffect · 向导稿 · 讨论记录 |

---

## 5. 给执行者的提醒

- **先 `--dry-run`** 确认索引可解析，再正式跑 gbx。
- 参数字段以 `effectExecutor.ts` 实际读取为准（§2.2 表），别照 UI 想当然。
- id 类字段一律下拉取画布/包/资源；空源时给 helperText 提示先创建。
- effect 面板按 effect 拆子组件；`ExitEffectsMockList` 只做列表 + 分发。
- 效果边必须与故事流转边视觉可区分（色/线型/标签）。
- 每 effect 子组件遵守 studio-v2 门禁：tab 缩进、props 入口解构 + 注释、引用注释、目录单元、无 barrel。
- 需要 git checkpoint：`gbx ... --checkpoint`（脏树会 BLOCKED）。
