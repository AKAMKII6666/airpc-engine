# L1 · 第一方能力包与流水线插槽 · 前置与分期执行索引

相关真源：[24-L1](../../技术设计文档/24-L1第一方能力包与流水线插槽.md) · [25-L2](../../技术设计文档/25-L2第三方能力包与SDK.md) · [19](../../技术设计文档/19-引擎宿主与会话模型.md) · [51](../../需求/51-自由通话增强-旧仓对标缺口.md) · [AGENTS.md](../../../../AGENTS.md)

**状态：** 1.1+ 架构；前置已 ✅；**L1-A～E 已收口**（对抗审查清零后可开 L2-0）。  
**本索引范围：** 文档/规则前置 + L1-A～E 工程分期。不含 L2 扫盘 / UI / ACL。

**定稿路径（24 §0）：**

| 项 | 路径 |
|----|------|
| 引擎第一方 | `packages/rpg-engine/src/capabilityPacks/` |
| Studio 装配 | `apps/studioV2/src/utils/server/capabilityPacks/` |
| 第三方（仅 L2） | `<workspace>/plugins/<id>/` |

---

## 禁止事项（全程）

- 引擎内核 `readdir(plugins)`；引擎内裸目录名 `plugins/`
- 往 `createEngineHost` / beginCall / endCall / effect switch **中段**堆特性
- 借 L1 抬高 `createEngineHost` / `effectExecutor` / `scheduleTick` 的结构 baseline 受控指标
- 把 51 的 F 项塞进**前置**；前置不做特性实现
- L1 阶段做 workspace `plugins/` 扫描、UI 三面板、ACL/沙箱
- Client / `pageComponents` / `bis` / `typeFiles` import 引擎或深挖 `capabilityPacks`
- 编辑 `docs/人类/`

**代码任务默认 verify：** `npm run quality:engine`（触及 Studio 装配时另加 `npm run quality:studio` 或至少 typecheck Studio）。

---

## 0. 前置（文档与规则对齐）

> 本阶段**无** `packages/rpg-engine/src/**` 行为代码变更。

| ID | 项 | 状态 |
|----|-----|------|
| PRE-1 | [24](../../技术设计文档/24-L1第一方能力包与流水线插槽.md) §0 拍板 + `capabilityPacks` 路径 + 前置后方可 L1-A | ✅ |
| PRE-2 | [25](../../技术设计文档/25-L2第三方能力包与SDK.md) L1/L2 分界；禁引擎裸 `plugins/` | ✅ |
| PRE-3 | [19](../../技术设计文档/19-引擎宿主与会话模型.md) §1.3 CapabilityPack 装配摘要 | ✅ |
| PRE-4 | [技术设计 00](../../技术设计文档/00-技术设计索引.md) / [需求 00](../../需求/00-文档索引.md) / [51](../../需求/51-自由通话增强-旧仓对标缺口.md) 口径同步 | ✅ |
| PRE-5 | [AGENTS.md](../../../../AGENTS.md) + codingRole / engine-boundaries / engine-code-quality 短约束 | ✅ |
| PRE-6 | 本执行索引落盘并链入 AGENTS | ✅ |

### 前置完成判据

- [x] 第一方落点统一为 `capabilityPacks`；workspace 第三方为 `plugins/`
- [x] 24 §0 含完整拍板表；19 / AGENTS / engine rules 含装配与禁扫约束
- [x] 写明：仅当前置全部勾选后才开 L1-A
- [x] 前置 diff 无引擎行为代码变更

**出口：** 前置全部 ✅ → 允许启动 L1-A。

---

## 1. L1 分期（前置收口后）

### L1-A · 槽点 TS 与 Pack 契约

| | |
|--|--|
| **内容** | 与 25 §5 同名的 Realtime/Background slot 常量或 union；`FirstPartyPack` / manifest 类型可编译；可建 `capabilityPacks/` 目录骨架（无业务特性） |
| **允许** | `packages/rpg-engine/src/capabilityPacks/**`（类型/常量）；门面按需导出类型；对应 `tests/` |
| **禁止** | 改 beginCall 语义；扫 plugins；实现 F-10 |
| **verify** | `npm run quality:engine` |
| **状态** | ✅ |

### L1-B · merge 装配 + compose 可开关

| | |
|--|--|
| **内容** | 独立 `mergeCapabilityPacks`；Studio `utils/server/capabilityPacks` 静态 import；`createEngineHost` **仅薄调用**；compose 样板可开关；Trace 可见 packId |
| **允许** | merge 新模块；server 装配；Host 一行级接线；测试夹具开关 packId |
| **禁止** | Host/effect/schedule 基线净增；Client import |
| **verify** | `npm run quality:engine`（+ Studio 相关检查） |
| **状态** | ✅ |

### L1-C · 样板包

| | |
|--|--|
| **内容** | F-10 user-location → `compose.providers`；outboundWindow → `schedule.gates` 样板 |
| **允许** | `capabilityPacks/realtime/user-location` 等；相关测 |
| **禁止** | 把逻辑写回 Host 中段 |
| **verify** | `npm run quality:engine` |
| **状态** | ✅ |

### L1-D · afterHangup + tasks 形状

| | |
|--|--|
| **内容** | `call.afterHangup` 可挂第一方钩子；`tasks.*` / `commit.*` 至少类型与可挂点 |
| **允许** | 钩子表/registry；薄 endCall 转调；接口形状 |
| **禁止** | 完整 L2 任务宿主产品化；伪造无卡外呼 |
| **verify** | `npm run quality:engine` |
| **状态** | ✅ |

### L1-E · core-compose

| | |
|--|--|
| **内容** | 默认 Prompt Providers 由 core-compose 包装；回归绿 |
| **允许** | `_core/` 包装；默认装配走 pack |
| **禁止** | 行为无故分叉；涨 Host 基线 |
| **verify** | `npm run quality:engine` |
| **状态** | ✅ |

### L1 收口判据（对齐 24 G1–G7）

- [x] 槽点类型化且与 25 同名
- [x] realtime / background 两大域
- [x] merge 可用；新增强默认走包
- [x] 至少 core-compose + 1 样板；可测开关/重复 id
- [x] **不**实现 workspace `plugins/` 扫描
- [x] 不要求 effects 全表化、不要求 UI

**出口：** L1 收口 → 可开 [25](../../技术设计文档/25-L2第三方能力包与SDK.md) L2-0。

---

## 文档维护

- 槽点/路径变更先改 [24 §0](../../技术设计文档/24-L1第一方能力包与流水线插槽.md)，再同步 25 与本索引。  
- 完成某分期后将对应 **状态** 改为 ✅，并更新收口判据勾选。
