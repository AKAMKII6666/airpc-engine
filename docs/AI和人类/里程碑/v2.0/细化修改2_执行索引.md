---
# Studio V2.0 · 细化修改 2 执行索引（gbx / general-batch-exe）
# 用法：node .cursor/skills/general-batch-exe/bin/gbx.js --exFile <本文件> --workdir <仓库根> [--dry-run]
# 本轮目标：User 补 gender/birthday/age → /users 改「玩家配置」→ AutoForm 完整档案表单 + user 段落盘。
# 前置：细化修改 1 全部 ✅ 后再跑本索引。

batch_size: 3
max_rounds: 60
max_fix_attempts: 5
max_ineffective_fixes: 2
stop_on_fail: true
group: order

verify_default:
  - npm run quality:studio

read_first:
  - AGENTS.md
  - .cursor/rules/codingRole.mdc
  - .cursor/rules/engine-boundaries.mdc
  - .cursor/rules/engine-code-quality.mdc
  - .cursor/rules/studio-v2-quality.mdc
  - docs/AI和人类/里程碑/v2.0/细化修改2_需求.md
  - docs/AI和人类/里程碑/v2.0/细化修改1_需求.md
  - docs/AI和人类/需求/10-用户与存档.md
  - docs/AI和人类/sudioV2.0版本/需求/ui/09-Studio-V2前端目录结构与落点规则.md
  - docs/AI和人类/sudioV2.0版本/需求/ui/08-Studio-V2前端工程规则与质量门禁需求.md

# hard_stop 写成「禁止动作」句，避免 Agent 总结「去掉打开调试器 / 未做 X」误伤
hard_stop_patterns:
  - "协议未定|需要人工决策|阻塞实现"
  - "实现年龄.*生日.*交叉|生日推算年龄|用年龄校验生日|用生日推算年龄"
  - "新增地理位置自由文本|把 location 写成自由串|location 自由文本字段"
  - "保留打开调试器按钮|新增调试器入口|本页导向 /debugger|调试器改版|改版 DebuggerShell"
  - "在玩家表单展示 Board|在玩家表单编辑 stories|callCards 进玩家表单"
  - "保留常用度主区|保留默认呼入角色主区|保留调试偏好主区|新增调试偏好主区"
  - "引入 Mithril 依赖|引入 jquery"

executor_extra: |
  只实现当前 ACTIVE / 本轮选出的 ⬜ 行；完成后把对应行改为 ✅。
  需求真源：细化修改2_需求.md。必须在细化修改 1 收口后执行；复用其 AutoForm，禁止另起裸 useState 表单。
  刀序 P1 → P2 → P3；引擎批 verify 用 npm run quality:engine。
  地理位置仅为分组标题；下面四国/省/市/区结构化字段；不做年龄↔生日交叉校验。
  编辑态展示字段全必填；创建与详情共用同一套字段配置。
  文案统一「玩家配置」；删除本页「打开调试器」按钮。
  不要改本索引 frontmatter 语义与已钉死口径；不要顺手改调试器模块。

reviewer_extra: |
  只读审查；禁止改业务代码。写出带本轮 reviewRunId 的 reviews/latest.json（见 general-batch-exe/docs/02-review-json.md）。
  对照 细化修改2_需求.md §3–§7。
  blocker/critical：仍称「调试用户」、仍有打开调试器主入口、表单含调试偏好主区、缺 gender/birthday/age、做了年龄生日交叉校验、地理位置自由文本、未用 AutoForm、引擎批未跑 quality:engine、门禁失败。

fixer_extra: |
  只根据审查报告与 latest-verify.json 修确认成立的问题；禁止顺手重构无关模块。
  不得用删注释、压行、大范围 eslint-disable、门禁 allowlist 掩盖结构问题。

workflow_dir: .ai-workflow-v2-refine2
adapter: table
---

# Studio V2.0 · 细化修改 2 执行索引

> **用法：**  
> `node .cursor/skills/general-batch-exe/bin/gbx.js --exFile docs/AI和人类/里程碑/v2.0/细化修改2_执行索引.md --workdir . --dry-run`  
> 通过后再去掉 `--dry-run` 正式跑。  
> **独立 workflow：** `.ai-workflow-v2-refine2/`（勿与 refine1 / step1 / step2 混用）。  
> **前置：** [细化修改1_执行索引.md](./细化修改1_执行索引.md) 任务全部 ✅。

本文回答：**细化修改 2（玩家配置）按什么顺序做、做到哪算完**。  
需求真源：**[细化修改2_需求.md](./细化修改2_需求.md)**。  
人类讨论：[原始需求讨论记录](../../../人类/原始需求讨论记录)（约 709–737 行）。

---

## 0. 已钉死口径

### 0.1 范围边界

| 做 | 不做（本轮） |
|----|----------------|
| `User` 增加 `gender` / `birthday` / `age` | 年龄↔生日交叉校验 |
| 导航/页头改为「玩家配置」；去掉打开调试器 | 调试器模块改版 |
| 详情+新建同一套 AutoForm 字段（§3.2） | 地理位置自由文本字段 |
| `user` 段经 API 落盘可回读 | 本页展示 Board/stories/world/schedule |
| 去掉常用度/调试偏好主区 | 登录、多存档槽、SaveGame 导入导出（可后续） |

### 0.2 与细化修改 1 的依赖

- 必须已有可用 AutoForm（`items[]` + 自动绑 + 逃生口）。
- 本轮 **不** 重做 AutoForm 基建；只消费它编排玩家表单。
- 若 AutoForm 不存在：停止并 BLOCKED，先完成 refine1。

### 0.3 推荐刀序

```text
P1-*  引擎 User 补字段
P2-*  文案与入口
P3-*  表单重编排 + 落盘
```

`group: order`：严格按表从上到下取 ⬜。

> **gbx 表解析注意：** 任务单元格勿写英文 `status`、勿写「状态」子串；避免同行同时触发 `id` 子串与「任务」误判。

### 0.4 引擎批落点声明（P1-*）

```text
行为目标：User 增加 gender(male|female)、birthday(YYYY-MM-DD)、age(number)；需求 10 同步；样例可回读
目标模块：packages/rpg-engine/src/schema/profile.ts（UserSchema）及门面导出
允许修改：上述 + 相关测试 + data/users 样例 + docs/AI和人类/需求/10-用户与存档.md
禁止落点：不得把玩家档案字段塞进 CharacterDef；不得改 CallSession/Effect 主链；不得在本批改 Studio UI
状态不变量：PlayerProfile 仍按 userId 隔离；无密码；Memory 仍不进 Profile JSON
回归测试：UserSchema 解析含新字段；缺字段仍可 parse（optional）；非法 gender 拒收（若用枚举）
verify：npm run quality:engine
结构预算：触碰基线文件时受控指标不得净增长
```

---

## 1. 任务表

| 状态 | ID | 任务 | verify |
|------|-----|------|--------|
| ✅ | V2-P1-1 | **UserSchema 补字段**：增加 gender（male/female）、birthday、age；更新类型导出 | npm run quality:engine |
| ✅ | V2-P1-2 | **需求 10 + 样例**：同步 10 §2；demo-user 等 profile.save.json 可含新字段且可加载 | npm run quality:engine |
| ✅ | V2-P2-1 | **文案玩家配置**：studioNavItems、/users 页头、角色库等指向 /users 的快捷链统一为「玩家配置」 | npm run quality:studio |
| ✅ | V2-P2-2 | **去掉调试器入口**：删除 UserLibraryHeader「打开调试器」；修正说明文案（非调试器附属定位） | npm run quality:studio |
| ✅ | V2-P3-1 | **详情 AutoForm 重编排**：按需求 §3.2 基本信息+地理位置分组+只读时间；编辑态全必填；移除常用度/调试偏好主区 | npm run quality:studio |
| ✅ | V2-P3-2 | **新建共用表单**：创建弹窗与详情同一套字段配置；系统生成 userId 与时间戳 | npm run quality:studio |
| ✅ | V2-P3-3 | **user 段落盘**：经 Next API 读/写 data/users 下 Profile 的 user 段；保存后回读一致 | npm run quality:studio |

---

## 2. 批次出口（每批结束后）

- 本批任务行均为 ✅
- 任务 `verify` / `verify_default` exit 0
- Reviewer：`{workflow_dir}/reviews/latest.json` 中本轮 `blocker=0` 且 `critical=0`

额外关注：

- **P1**：gender 枚举是否钉死 male/female；需求 10 是否已改
- **P2**：侧栏是否仍显示「调试用户」；是否仍有打开调试器按钮
- **P3**：新建是否仍简陋；是否做了年龄生日交叉校验；是否展示 Board 等经历态

---

## 3. 本轮总出口（全部 ✅ 后）

- [ ] [细化修改2_需求.md](./细化修改2_需求.md) §7 可勾选
- [ ] `npm run quality:engine` 与 `npm run quality:studio` 按改动面全绿
- [ ] 导航与页头为「玩家配置」；无本页调试器主入口
- [ ] 详情/新建字段与引擎 `User` 对齐；无旧调试偏好主区
- [ ] 未改调试器业务模块

收口后 **不要在本文件继续加任务**。

---

## 4. 任务 ↔ 需求映射

| 任务组 | 细化修改2_需求 | 其它 |
|--------|----------------|------|
| V2-P1-* | §3.3 · §4.1 | 需求 10 · quality:engine |
| V2-P2-* | §3.1 · §4.2 | studioNavItems · Header |
| V2-P3-* | §3.2 · §3.5–3.6 · §4.3 | AutoForm（细化修改 1） |

---

## 5. 给执行者的提醒

- **先确认 refine1 已 ✅**，再开本索引。
- 地理位置 = 分组标题，不是第五个自由文本。
- 男/女 → `male`/`female`；不要引入角色侧的 non_binary（本轮 UI 仅两档）。
- 需要 git checkpoint：`gbx ... --checkpoint`（脏树会 BLOCKED）。
