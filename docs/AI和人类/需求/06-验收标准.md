# 06. 验收标准

> **E11（2026-07-15）收口：** [100% 计划](../里程碑/对照本仓库文档的工程落地_100计划.md) E0–E11 工程项已与实现／测对齐勾选。  
> 电话壳／Realtime／真硬件／旧话机迁移等见「非本仓分母」；**不宣称维护者产品体验验收已签**（整体效果由维护者自测后再开 1.1）。

## 1. 引擎（`packages/rpg-engine`）

- [x] User / PlayerProfile 创建、序列化；无多存档槽
- [x] 剧情 / Board / facts / `redialSlot` 在薄 Profile（JSON）；**记忆在 SQLite**（经 MemoryPort），不在 `profile.save.json` 大数组
- [x] CharacterDef + social + `effectiveDialable`（unlocked 可覆盖）+ `set_character_unlocked`
- [x] ActiveStoryLock 决策表（见 19）；Resolver 读锁（T1：hard reject / soft force_free / begin 激活 StorySave / end_story 释锁；外呼 hard 拒／soft 打标）
- [x] CallCard schema / validatePackage；layout 旁车忽略（E3：`ASSET_URI_MISSING` + 08 warning 主路径进 `VALIDATE_PACKAGE_ERROR_COVERAGE.covered`；`deferred` 空）
- [x] ToolRegistry：七支业务 + `search_memory`/`get_memory_by_id`；挂机三支不进；Free `inherit_free`
- [x] RuntimeExitCandidate；工具不直接 Effect
- [x] CallSession schema；frozenCard；Free `packageId=__free__`；同 user 通话互斥
- [x] Resolver：pending > Free；priority；schedule = clock+minMs（无锁语义，锁见 T1）
- [x] ExitSelector：静态 ∪ 动态；同 priority 静态优先（T11：专测静态同 prio 压动态）
- [x] Free → FreeCallPostPipeline（MemoryCommit；有候选再 Exit）；禁隐式 transcript 扫描
- [x] Free + `refer_to_expert` candidate → 挂机后 unlock/schedule 落盘（E4a：`refer-to-expert-e2e`）
- [x] Effect + EffectSink 时序；无 Relay（T2：`EffectSink` 接口＋Noop／Recording 桩；媒介 effect 先 WET 再 Sink；无真硬件）
- [x] MemoryCommit 契约（12）：episodic 通末总结 + 生活 vignette 可写 SQLite；**必带时间戳**；失败不阻断已执行 Effect（E5：端口／投影有；Free 默认仍 `call_summary`；**LLM 自动抽取 vignette = 非本计划分母／1.1**）
- [x] `projectForCall` 有条数/字符预算；禁止 beginCall 全表灌 prompt
- [x] `search_memory` / `get_memory_by_id`：`session_local`、硬 limit、FTS 查库（非向量）；Hit≤200 / 全文≤500；不扫 JSON 文件
- [x] Commit 后可增量 `rollupIfNeeded`；不在 beginCall 热路径全量重算（E5：真实现＋测）
- [x] Effect plan：失败继续、critical 中止；ledger 可测（T2：`EffectPlanResult.status` + critical／中段失败测）
- [x] 事件序 + `data/logs/*.jsonl`（T9：ring + 脱敏落盘 + `/api/logs`）
- [x] patch_memory / fact / knowledge 跨角色仅出口
- [x] schemaVersion 拒载；错误码表（19）；[08](./08-内容校验规则.md) 总表（校验 `SCHEMA_UNSUPPORTED`＋Studio Dialog／T6；`VALIDATE_PACKAGE_ERROR_COVERAGE.deferred` 空）
- [x] Composer：`ComposeScene`/`RenderedPrompt`/`DialogueSessionSpec`；本地时间；patch 不得盖 objective/forbidden（E4d：night／late_night 样例＋测）
- [x] 端口可替换；零 React

## 2. Studio（B/S）

- [x] `/stories` 列表 → UserGate → `/stories/[id]` 画布（T6：打开／创建／直达画布均经 Gate）
- [x] UserGate 组件复用于调试器；无密码
- [x] **仅 MUI** + **CSS Modules/SCSS**；`@xyflow/react` 画布
- [x] **Zustand + bis + shell**（[21](../技术设计文档/21-Studio客户端分层.md)）：page 不堆业务 fetch；store 无网络（主路径已用；个别导出直调 ajax 可接受）
- [x] client 无引擎写口；LLM Key 仅 server；Host 单例
- [x] 调试与编辑分离；调试中不改卡
- [x] 调试器：Board / effect / candidates / log；本地时间覆盖；matchedLayerIds；过程话术模拟按钮（T8）
- [x] author / playtest；导出无 privateBrief（T9）
- [x] `/api/debug/*` SSE（E8：`POST /api/debug/chat` 为 `text/event-stream`；refreshStamp 可兜底）；SCHEMA 拒载 Dialog；API 草表见 [18](../技术设计文档/18-部署拓扑与BS架构.md)
- [x] `/world` 世界台：Lore／Facts／knowledge／schedule 读写（E1）
- [x] `/assets` 资源台 + assets 全链路（E3）
- [x] 画布 P1：搜索／复制粘贴／自动布局（E2）
- [x] 文本对话调试 chat（E7）＋ Clock／Tick 模拟器（E9）＋ WET 查询／受控追加／重放（E10）

## 3. Golden 场景

- [x] 引荐：挂卡 + unlock + redial（无 relay）；样例 `data/storis-packages/golden_handoff/`
- [x] 动态出口登记 → 挂机命中
- [x] Free 通话 → PostPipeline 记忆总结
- [x] A 出口写 B 记忆/knowledge（E4b：`doubao_a_writes_b`＋`a-to-b-memory-e2e`）
- [x] effect 中段失败 → completed_with_errors（T2：plan／session 可标 `completed_with_errors`）
- [x] schedule once 快进；recurring 写入 intents；切换 userId 隔离（E4c＋E9）
- [x] research → Profile.research.commitments（T11：executor 测）
- [x] Lore bootstrap / fallback（T10）；真 LLM 可注入、失败仍 fallback（E6）
- [x] 同卡 outbound + 夜间 layer 命中；Composer 含本地时间块（E4d）

## 4. 文档

- [x] `00` 含技术设计分册与 `promptScenes` / 本地时间铁律口径
- [x] `data/storis-packages/`：`story.conf.json` + 分文件 `.s-card.json`；按需载入；golden_handoff 样例存在
- [x] 全局 `data/assets/`（资源台 + `ASSET_*` 校验）；包无 agents[] / 无内联 assets[] 真源（`ASSET_PACKAGE_INLINE` warning）
- [x] [19](../技术设计文档/19-引擎宿主与会话模型.md) 与 `03`/`04`/`11`/`50`、技术设计 `05`/`18` 一致
- [x] [11](./11-角色与社交.md) 含 identity / persona / defaultPromptScenes / callFlowPrompts / Free 路径 / unlocked / narrative-only
- [x] [10](./10-用户与存档.md) 含 ScheduledIntent once|recurring 与 research.commitments
- [x] [50](./50-对话与模型适配.md) 含 ComposeScene / RenderedPrompt / DialogueSessionSpec；过程话术计时归属
- [x] [13](./13-工具与能力注册.md) 含对标旧话机的 v1 工具、壳侧挂机 FC 边界、inputSchema=Zod 重写（不搬旧文件）
- [x] [08](./08-内容校验规则.md) 与 [15](./15-动态出口与通话中工具事件.md) Free 分叉一致
- [x] Free：`__free__` 哨兵；PostPipeline 步骤与「禁隐式扫描」一致
- [x] 技术文在 `技术设计文档/`，需求文在 `需求/`（不混放）

## 5. 非本仓分母（保持空勾或不宣称）

以下**不算**「对照本仓文档的工程落地 100%」漏项：

- [ ] 电话壳／Realtime／真硬件 EffectSink（另建壳仓）
- [ ] 旧 doubaoSister 迁移／对标验收签字
- [ ] 向量语义检索／embedding（20 明确定后置）
- [ ] LLM 自动从 transcript 抽取生活 vignette（E5 端口已闭合；接线属 1.1）
- [ ] 维护者「好不好看／好不好用」整体体验签字（→ 1.1）
