# 过程岔子与纠正记录（Agent 记忆）

> 本文件记录「做过什么偏了、用户/门禁怎么要求改回来」。  
> 目的：换会话后不要重复踩坑。  
> 固化：2026-07-22。条目按主题，时间粗略落在 2026-07 中下旬本轮 Studio V2 / 细化修改7 相关工作。

---

## A. 画布选中与属性浮窗

### A1. 框选也打开属性浮窗（会崩 / 体验错）

- **偏差：** 多选节点时仍推进 CallCard 属性浮窗；缺字段时偶发炸。  
- **要求：** 框选**不要**开属性面板；只有**单选**才开。  
- **纠正：** `storyCanvasSelection.ts`：`params.nodes.length === 1` 才取节点；`toolPolicy?.mode ?? ""` 等防御。  
- **记忆：** 多选 = 画布操作态，≠ 编辑单卡合同。

---

## B. 浮窗与表单 UX

### B1. 浮窗拖拽坐标错位

- **偏差：** 用 viewport/`clientX` 直接当 left/top，相对定位父级时偏移。  
- **纠正：** `useFloatingPanelDrag.ts`，坐标相对 `offsetParent`。  
- **记忆：** FloatingPanel 是 absolute/相对壳层，不是全屏 fixed 假设。

### B2. 挂载目标弹层字段过重

- **偏差：** Mount 弹层暴露包 / activation / 手填 agent 等。  
- **要求：** 只要选**目标卡**；`agentId` 从卡归属自动带出；排除自身。  
- **纠正：** Mount 相关面板精简（见 ExitListEditor `mount/`）。  
- **记忆：** 演示期优先「连到哪张卡」一眼能懂。

### B3. 表单拥挤

- **要求：** 间距、小时区间 nowrap、modal 宽度与 padding-top 等微调。  
- **纠正：** `LocalHourRangeField`、ExitList / FloatingPanel scss 等局部调整。  
- **记忆：** 向导稿优先可读，不堆控件。

---

## C. 角色 mock 与落盘边界

### C1. 差点写成磁盘角色（历史 · 细化阶段）

- **上下文：** 要对齐正式稿五人。  
- **当时口径：** **仅会话 mock**，先不写 `data/characters`。  
- **当时纠正：** `MOCK_CHARACTERS` + 角色库读 mock；编辑器锚点对齐同一 agentId 集合。  
- **记忆：** 「能看见五人」≠ 「Content 已落盘」（该阶段成立）。

### C2. 角色库曾刻意不走 API（历史）

- **当时纠正：** `useCharacterLibraryPage` 注明列表真源为会话 `MOCK_CHARACTERS`。  
- **记忆：** 接真 API 须显式切换。

### C3. V2-T3-3：角色 CRUD 接通并删 mock（当前）

- **纠正：** 列表/新建/删除/详情与编辑器角色弹窗全部 `/api/characters` ↔ `data/characters`。  
- **同一任务内删除** `MOCK_CHARACTERS` / `mockCharactersData` / `listMockCharacters` / `commitUpdateCharacterMock` 等业务入口与无引用文件；禁止 `export {}` 空壳残留。  
- **记忆：** 角色域已是磁盘真源；勿再按 C1/C2 写代码。

---

## D. personalityCode / MBTI

### D1. 引擎先于 Studio

- **顺序（用户）：** 先改引擎实现，再 mock UI。  
- **引擎：** schema + Composer hard 注入。  
- **Studio：** Select + mock 五人填码。  
- **记忆：** 跨柱字段先定引擎契约，再绑 UI。

### D2. `ENGINE-STRUCT-007` 基线净增长

- **偏差：** 直接在 `composer.ts` / 原测试文件加人格逻辑与用例，有效行 / 复杂度超触碰基线。  
- **纠正：**  
  - 抽出 `personalityPrompt.ts`  
  - 测试放到 `composer.personality.test.ts`  
  - 门面从 personalityPrompt 导出  
- **记忆：** 触碰引擎遗留超限文件时，**只能持平或下降**；新能力优先新文件。

### D3. runtime 平铺告警

- 新增 `personalityPrompt.ts` 使 `src/runtime` STRUCT-009 组名更多；当前为 **warning**。  
- **记忆：** 下次大改 compose 相关时应考虑 `runtime/compose/` 聚类，但勿为单文件建空壳目录。

---

## E. 故事第一章 mock

### E1. 旧「内存条事件」占位

- **偏差：** 画布三卡讲内存条 / 小皮库存，与正式第一幕无关。  
- **要求：** 对齐 `1.第一幕初识.md`（打错电话），纯前端看效果。  
- **纠正：** 整图替换为误拨 → 补打 → 留言/完结；包展示名改「第一幕：打错电话」。  
- **刻意保留（历史）：** 曾保留 `packageId = pkg_memory_bar_1` 避免路由大迁；**T3-6 后**正式包 id 已为 `wrong_number_act1`。
- **用户反馈：** 「稍微有点偏差，但是很准确了」——压缩台词与出口简化可接受；后续精修另开任务。

### E2. `STUDIO-STRUCT-017` 空格缩进

- **偏差：** 改 `mockWorkbenchData.ts`（历史空格缩进）只改内容，门禁要求触碰即改 tab。  
- **纠正：** 整文件改 tab。  
- **记忆：** V2「触碰即整改缩进」是硬规则。

### E3. CanvasMockGraph 行数告警

- **偏差：** 节点+边写一文件 → 有效行 > 350 告警。  
- **纠正：** 边拆到 `CanvasMockGraph.edges.ts` 再 export。  
- **记忆：** mock 图也会涨；叙事数据可再拆「卡数据 / 边数据」。

---

## F. 细化修改 7 / gbx（相关上下文）

- 脏树 + `--checkpoint` 可能导致 BLOCKED；可用 `--clear-blocked`（用户侧运维经验）。  
- Effect 面板与挂载边是本阶段编辑器「站稳」的能力底座；第一章 mock 是在其上的**内容演示**。

---

## G. 真源接通（第三步）

### G1. 玩家 mock 零残余（V2-T3-4）

- **偏差风险：** 列表早已走 `/api/users`，但 `mockLibraryData` 仍留 `MOCK_USER_PROFILES` / `listMockUserProfiles` 等无引用入口。  
- **要求：** 接通验收的同一任务内删玩家列表 mock；禁止「先接上、mock 留着」。  
- **纠正：** 删除上述残余；`buildMockUserFromForm` → `buildUserFromForm`（避免 Mock 命名暗示会话真源）。  
- **记忆：** 业务路径已 API 不等于清零完成；须 `rg` 确认无业务入口残留。

### G2. 故事包 UI + 编辑接线（V2-T3-6 / T3-7 / T3-8）

- **纠正：** 列表 `listStoryPackages_bis` 扫磁盘；编辑器 `loadStoryPackage_bis` / `saveStoryPackage_bis` + `diskBundleGraph`；删除 `CanvasMockGraph`、`MOCK_STORY_PACKAGES` 业务真源、`MOCK_EDITOR_CHARACTERS`、`ExitEffectsMockList`。  
- **packageId：** 正式第一幕改为 `wrong_number_act1`（非 `pkg_memory_bar_1`）。  
- **记忆：** 会话图编辑后须顶栏整包保存才落盘；`docs/AI` 在 T3-8 回写真源口径。

---

## H. 文档层本身

- `docs/AI/` 曾为空；2026-07-22 用户明示作为 **Agent 记忆真源库** 后才批量固化。  
- 治理：未明示不得把内容升格进 `docs/AI/`；T3-8 已回写第三步真源收口事实。
