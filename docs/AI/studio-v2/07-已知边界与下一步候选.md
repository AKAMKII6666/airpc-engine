# 已知边界与下一步候选（代码事实）

> 固化：2026-07-22（V2-T3-8：第三步真源接通已收口）。  
> 「站稳」指：角色/玩家/故事包列表/编辑器读写+编辑接线均可磁盘真源；**不等于** Host 闭环或资源库落盘完成。

## 1. 故意未做 / 尚未做

| 项 | 现状 | 勿误判 |
|----|------|--------|
| 角色落盘 | **已做**：`data/characters` ↔ `/api/characters` | 勿再写「仅 MOCK_CHARACTERS」 |
| 玩家落盘 | **已做**：`data/users` ↔ `/api/users` | 勿再写「仅 MOCK_USER_PROFILES」 |
| 故事包 BFF + UI | **已做**：列表扫盘；编辑器打开/整包保存；`CanvasMockGraph` 已删 | 勿再写「打开仍走内存种子」 |
| 编辑接线落盘 | **已做**：浮窗/出口/Effect/归属经整包保存写 cards+layout | 不等于 Host 真跑 Effect |
| Host 跑通第一幕 | 无 | 出口/Effect 仍是编辑器写 Content |
| 资源库落盘 | 仍 `MOCK_ASSETS` / `listMockAssets` | 本步允许暂留 |
| 调试器真会话 | 仍 `mockDebuggerData` | 本步允许暂留 |
| 正式稿逐句剧本 | speakable 压缩 | 不是完整剧本导入器 |
| 小皮文末通话卡 | 只在 chapter_end 文案 | 下章钩子未做成入口卡 |
| 条件 DSL | 出口用人话 conditionSummary | 无结构化谓词编辑器 |
| 音色枚举补全 | 部分磁盘 voiceId 不在枚举 | 与 MBTI 无关的债 |
| `pkg_memory_bar_1` | 仅调试器/设置/部分测试夹具 | 正式包 id = `wrong_number_act1` |

## 2. 已站稳、可依赖的能力

- 单选开属性浮窗；框选不开。  
- CallCard / 章节属性 Formik；出口列表 + 分 effect 面板（`ExitEffectsList`）。  
- attach 效果边与 effects 行同步；保存后刷新仍在。  
- 角色库 / 画布角色弹窗：人设 + **MBTI Select**；读写经 `/api/characters`。  
- 玩家配置页：列表/新建/详情/删除经 `/api/users`。  
- 故事包：`GET/PUT /api/stories`；`wrong_number_act1` 三卡 + layout 可读可写。  
- 引擎：`personalityCode` → Composer systemHard（有单测）。  

## 3. 下一步候选（第三步已收口）

第三步里程碑已完成，见 [项目第三步计划执行索引](../../AI和人类/里程碑/v2.0/项目第三步计划执行索引.md)（T3-1–T3-8 ✅）。

后置项（勿自动开干）：

1. 资源库落盘 / 导入导出正式管道  
2. runtime/compose 目录聚类（消 STRUCT-009）  
3. 第二幕内容包  
4. 调试器真会话 / Host 闭环  

## 4. 改代码前必读

1. [00-Studio-V2代码事实索引](../00-Studio-V2代码事实索引.md)  
2. [06-过程岔子与纠正记录](./06-过程岔子与纠正记录.md)  
3. 若再动真源域 → 对照 [第三步需求](../../AI和人类/里程碑/v2.0/第三步需求.md) §2.0 零残余  
4. 动完若改变「代码事实」→ **回写本目录**
