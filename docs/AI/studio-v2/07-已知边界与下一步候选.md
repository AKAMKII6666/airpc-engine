# 已知边界与下一步候选（代码事实）

> 固化：2026-07-23（V2-S8-19：调试器读盘 validate 展示）。  
> 增量：2026-08-17 — 调试器真会话、Host 闭环、MemoryCommit Orchestrator 已落地；见 [09](./09-记忆Commit与Trace.md)。  
> 「站稳」指：角色/玩家/故事包列表/编辑器读写+编辑接线+保存校验均可磁盘真源。

## 1. 故意未做 / 尚未做

| 项 | 现状 | 勿误判 |
|----|------|--------|
| 角色落盘 | **已做**：`data/characters` ↔ `/api/characters` | 勿再写「仅 MOCK_CHARACTERS」 |
| 玩家落盘 | **已做**：`data/users` ↔ `/api/users` | 勿再写「仅 MOCK_USER_PROFILES」 |
| 故事包 BFF + UI | **已做**：列表扫盘；编辑器打开/整包保存；`CanvasMockGraph` 已删 | 勿再写「打开仍走内存种子」 |
| 编辑接线落盘 | **已做**：浮窗/出口/Effect/归属经整包保存写 cards+layout | 不等于 Host 真跑 Effect（Host 已另接） |
| 保存 validate | **已做**：PUT 写后 `validatePackage`；error 回滚阻断；条幅定位到 card | warning 默认不阻断；exit 级仅 path 展示 |
| 资源库落盘 | **已做**：`data/assets` ↔ `/api/assets`（细化修改 8） | 勿再写 MOCK_ASSETS 业务真源 |
| 调试器读盘 validate | **已做**：`GET /api/stories/:id/validate` + `/debugger` 面板展示 | 已不等于「无 Host」 |
| 调试器真会话 / Host | **已做**：`beginCall` / chat / `endCall` / Free PostPipeline | 勿再写「通话叙事区仍 mock」 |
| MemoryCommit 抽取 | **已做**：完整 transcript → 结构化字段 → Port 分层落库 | 勿再写「只写 call_summary、LLM 抽取属 1.1」 |
| Memory Trace | **半成品**：DTO + overlay 预览；成品面板见 [10](./10-下一步-态度记忆与MemoryTrace面板.md) | 不要重做 API/DTO |
| 态度记忆 | **未做** | 不要塞进 userFacts；开工单见 [10](./10-下一步-态度记忆与MemoryTrace面板.md) |
| 正式稿逐句剧本 | speakable 压缩 | 不是完整剧本导入器 |
| 小皮文末通话卡 | 只在 chapter_end 文案 | 下章钩子未做成入口卡 |
| 音色枚举补全 | 部分磁盘 voiceId 不在枚举 | 与 MBTI 无关的债 |
| `pkg_memory_bar_1` | 仅 mock 会话文案 / 设置夹具 | 正式包与校验默认 id = `wrong_number_act1` |

## 2. 已站稳、可依赖的能力

- 单选开属性浮窗；框选不开。  
- CallCard / 章节属性 Formik；出口列表 + 分 effect 面板（`ExitEffectsList`）；ExitCondition v1 写 `exit.condition`。  
- attach 效果边与 effects 行同步；保存后刷新仍在。  
- 角色库 / 画布角色弹窗：人设 + **MBTI Select**；读写经 `/api/characters`；全库锚点。  
- 玩家配置页：列表/新建/详情/删除经 `/api/users`。  
- 故事包：`GET/PUT /api/stories`；PUT 经 `writeValidatedDiskStoryPackage`；只读 `GET …/validate`。  
- 调试器：选磁盘包可展示 `validatePackage`；真会话走 Host `beginCall` / chat / `endCall`；挂机 toast + 副作用 overlay；Memory Trace DTO。  
- 引擎：`personalityCode` → Composer systemHard（有单测）；路径 B 引用角色校验。  
- 记忆：Studio Orchestrator 抽取；Sqlite Port 分层落库；完整 transcript，禁止 user-only 门闩。  

## 3. 下一步候选

细化修改 8（含可选 S8-19）已收口。

后置项（当前开工单：[10-下一步-态度记忆与MemoryTrace面板.md](./10-下一步-态度记忆与MemoryTrace面板.md)）：

1. 态度记忆（NPC 对玩家的态度轴）
2. 独立 Memory Trace 成品面板（现有 overlay + DTO 不够）
3. runtime/compose 目录聚类（消 STRUCT-009）
4. 第二幕内容包
5. validate 定位增强（exit 行高亮、包级 conf 浮窗跳转）

## 4. 改代码前必读

1. [00-Studio-V2代码事实索引](../00-Studio-V2代码事实索引.md)  
2. [06-过程岔子与纠正记录](./06-过程岔子与纠正记录.md)  
3. 若再动真源域 → 对照 [第三步需求](../../AI和人类/里程碑/v2.0/第三步需求.md) §2.0 零残余  
4. 动完若改变「代码事实」→ **回写本目录**
