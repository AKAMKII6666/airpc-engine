# 已知边界与下一步候选（代码事实）

> 固化：2026-07-23（V2-S8-19：调试器读盘 validate 展示）。  
> 「站稳」指：角色/玩家/故事包列表/编辑器读写+编辑接线+保存校验均可磁盘真源；**不等于** Host 闭环。

## 1. 故意未做 / 尚未做

| 项 | 现状 | 勿误判 |
|----|------|--------|
| 角色落盘 | **已做**：`data/characters` ↔ `/api/characters` | 勿再写「仅 MOCK_CHARACTERS」 |
| 玩家落盘 | **已做**：`data/users` ↔ `/api/users` | 勿再写「仅 MOCK_USER_PROFILES」 |
| 故事包 BFF + UI | **已做**：列表扫盘；编辑器打开/整包保存；`CanvasMockGraph` 已删 | 勿再写「打开仍走内存种子」 |
| 编辑接线落盘 | **已做**：浮窗/出口/Effect/归属经整包保存写 cards+layout | 不等于 Host 真跑 Effect |
| 保存 validate | **已做**：PUT 写后 `validatePackage`；error 回滚阻断；条幅定位到 card | warning 默认不阻断；exit 级仅 path 展示 |
| 资源库落盘 | **已做**：`data/assets` ↔ `/api/assets`（细化修改 8） | 勿再写 MOCK_ASSETS 业务真源 |
| 调试器读盘 validate | **已做**：`GET /api/stories/:id/validate` + `/debugger` 面板展示 | **不等于** Host / beginCall |
| Host 跑通第一幕 | 无 | 出口/Effect 仍是编辑器写 Content |
| 调试器真会话 | 通话叙事区仍 mock | 不 Host beginCall；勿把 mock 通话当真会话 |
| 正式稿逐句剧本 | speakable 压缩 | 不是完整剧本导入器 |
| 小皮文末通话卡 | 只在 chapter_end 文案 | 下章钩子未做成入口卡 |
| 音色枚举补全 | 部分磁盘 voiceId 不在枚举 | 与 MBTI 无关的债 |
| `pkg_memory_bar_1` | 仅 mock 会话文案 / 设置夹具 | 正式包与校验默认 id = `wrong_number_act1` |

## 2. 已站稳、可依赖的能力

- 单选开属性浮窗；框选不开。  
- CallCard / 章节属性 Formik；出口列表 + 分 effect 面板（`ExitEffectsList`）；ExitCondition v1 写 `exit.condition`。  
- attach 效果边与 effects 行同步；保存后刷新仍在。  
- 角色库 / 画布角色弹窗：人设 + **MBTI Select**；读写经 `/api/characters`；全库锚点。  
- 玩家配置页：列表/新建/详情/删除经 `/api/users`。  
- 故事包：`GET/PUT /api/stories`；PUT 经 `writeValidatedDiskStoryPackage`；只读 `GET …/validate`。  
- 调试器：选磁盘包 → 展示 `validatePackage` errors/warnings；会话叙事仍 mock。  
- 引擎：`personalityCode` → Composer systemHard（有单测）；路径 B 引用角色校验。  

## 3. 下一步候选

细化修改 8（含可选 S8-19）已收口。

后置项（勿自动开干）：

1. 调试器真会话 / Host 闭环  
2. runtime/compose 目录聚类（消 STRUCT-009）  
3. 第二幕内容包  
4. validate 定位增强（exit 行高亮、包级 conf 浮窗跳转）

## 4. 改代码前必读

1. [00-Studio-V2代码事实索引](../00-Studio-V2代码事实索引.md)  
2. [06-过程岔子与纠正记录](./06-过程岔子与纠正记录.md)  
3. 若再动真源域 → 对照 [第三步需求](../../AI和人类/里程碑/v2.0/第三步需求.md) §2.0 零残余  
4. 动完若改变「代码事实」→ **回写本目录**
