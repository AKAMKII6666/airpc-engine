# E2E 报告 · 2026-09-10（R2-DBG-030～033 FC 补跑）

## 元信息

| 项 | 值 |
|----|-----|
| 日期 | 2026-09-10 |
| 执行人 / Agent | Auto（新 Key 后 FC 补跑 → 修 → 再测） |
| Studio | http://localhost:8562 |
| userId | demo-user（测后已恢复 nickname=小陈） |
| LLM | qwen3.5-flash · 新 Key |

## 结果

| ID | 结果 | 证据摘要 |
|----|------|----------|
| R2-DBG-030 | **Pass** | `record_user_name` 成功；args `nickname=测测`；候选出口可见 |
| R2-DBG-031 | **Pass** | `search_memory` 成功（`text_query=用户名字昵称`，no_hits）；口头合理解释「测测」 |
| R2-DBG-032 | **Pass** | `schedule_reminder_call` 成功（`delay_minutes=2`，topic 提醒喝水）；约 2 分钟后出现白半仙呼入 |
| R2-DBG-033 | **Pass** | `request_hangup` 成功（llm log `finish=tool_calls`）；回待机；toast 可能已闪过未截到 |

## 本轮产品修复（为通过 FC）

1. **有 tools 时关 thinking**（`debuggerToolCalling.server.ts`）：避免 qwen3.5-flash 默认思考导致口头答应不调工具。
2. **明显 FC 意图首轮 `tool_choice=required`**（记名/预约回电/主动挂机；搜记忆不强制，以免 XML 假调用）。
3. **正文 XML tool_call 回收**（`debuggerToolCallTextRecovery.server.ts`）：required 偶发把 FC 打成 `<tool_call>` 正文时解析为正式 toolCalls。
4. **剧本收紧**：预约/记名「禁止只说不调」；熟人辨认允许不同新昵称走改记。
5. **shell 指令补 `[tool-discipline]`**。

## 单测

```bash
npm run test -w @airpc/studio-v2 -- --run \
  tests/debugger/debuggerToolCalling.test.ts \
  tests/debugger/debuggerToolCallTextRecovery.test.ts
npm run test -w @airpc/rpg-engine -- --run tests/tools/toolInstructionBlocks.test.ts
```

均通过。

## 污染清理

- 已 `git checkout` 恢复 `demo-user/profile.save.json`（测中曾被改成 nickname=测测）与 `post-call-jobs.json`。
- 已重启 `dev:v2`。

## 结论

R2-DBG-030～033 在新 Key 下补跑通过；退出条件满足。原「欠费 Blocked」关闭。
