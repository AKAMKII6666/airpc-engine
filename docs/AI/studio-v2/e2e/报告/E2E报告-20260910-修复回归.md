# E2E 报告 · 2026-09-10（表项修复回归）

## 元信息

| 项 | 值 |
|----|-----|
| 日期 | 2026-09-10 |
| 执行人 / Agent | Auto（修复→E2E 循环） |
| Studio 入口 | http://localhost:8562（`npm run dev:v2`） |
| userId | demo-user |
| 范围 | 修复表 #1～#5 对应路径；不含 LLM 欠费阻塞的 FC 深测 |

## 修复对照

| # | 问题 | 代码落点 | 回归结果 |
|---|------|----------|----------|
| 1 | free 首通 softContext 含 memory / inertia | `promptTraceProject.server.ts`、`debuggerLlmMessages.server.ts` | **Pass** |
| 2 | 编辑器「正在从磁盘加载…」卡死 | `storyEditor.shell.bis.ts`（世代号 + try/catch） | **Pass** |
| 3 | `StudioAppChrome` hydration 干扰 | `StudioAppChrome.tsx`（首帧非全屏再切） | **Pass**（导出表单可直达） |
| 4 | 性别 Select 空值误显「男」 | `FormSelectField/index.tsx`（`renderValue`） | **Pass**（DOM 文案「请选择」；a11y name 仍可能读「男」） |
| 5 | 「自由通话」chip 无 onClick | Idle 上下文 chip → `dialFreeCall` | **Pass**（点 chip 拨 010） |

## 用例结论（本轮）

| ID | 结果 | 证据摘要 |
|----|------|----------|
| R2-DBG-022 | **Pass** | chip 拨白半仙；首句「喂？请问哪位？」；Trace `soft 0`；softContext 空；无 `[memory]` / `[conversation.inertia…]` 内容块（providers 列表仍可出现 `conversation.inertia` 名，不计入注入） |
| R2-ED-010 路径 | **Pass（加载解锁）** | 章 URL 可出 UserGate → 选 demo-user → 画布（Zoom/新建 CallCard 等）；不再永真 loading |
| R2-PKG-040 相关 | **Pass** | `/packages/export` 表单（故事包/用途）可见，无需先清 overlay |
| R2-LIB-U-010 展示 | **Pass** | 新建玩家对话框性别折叠态 DOM=`请选择`，`input[name=gender]=""` |
| free chip | **Pass** | 摘机后点「自由通话」→ `当前号码：010` → 接通 |

## 单元测

```bash
npm run test -w @airpc/studio-v2 -- --run \
  tests/debugger/promptTraceOpeningIsolation.test.ts \
  tests/debugger/debuggerLlmMessages.test.ts
```

结果：6 passed。

## 未纳入本轮退出条件

- R2-DBG-030～033（LLM 欠费 / `fetch failed`）— 环境问题，非表项产品缺陷。
- R2-ED-031～042 属性浮窗深测 — 加载阻塞已解除；本轮仅验证进画布，未重跑字段保存全矩阵。

## 结论

表项 #1～#5 修复后，对应 E2E 路径均已复测通过。原 R2 **Fail 1（DBG-022）** 与 **编辑器加载 Blocked** 在本回归中关闭。
