# stores/packages

包列表 / 导入导出域账本（V2-LY-9）。

- 实现：`packagesStore.ts`（列表 / 选中 / loading / refreshStamp）
- 灌账：`bis/shellBis/packages/packages.shell.bis.ts`
- 会话：`bis/pageBis/packages/list/packageListSession.bis.ts`

禁止在此目录发请求或 import bis / ajaxProxy。
