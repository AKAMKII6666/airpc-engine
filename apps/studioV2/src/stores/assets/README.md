# stores/assets

资源库域账本（V2-LY-8）。

- 实现：`assetsStore.ts`（列表 / 选中 / loading·error / refreshStamp）
- 灌账：`bis/shellBis/assets/assets.shell.bis.ts`
- CRUD 编排：`bis/pageBis/assets/list/assetLibrarySession.bis.ts`

禁止在此目录发请求或 import bis / ajaxProxy。
