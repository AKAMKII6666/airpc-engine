/**
	* 设置路由：只做装配。
	*/
import { SettingsShell } from "@studio-v2/src/pageComponents/settings/SettingsShell";

export default function SettingsPage() {
	// 引用了SettingsShell组件，用于页面展示与交互
	return <SettingsShell />;
}
