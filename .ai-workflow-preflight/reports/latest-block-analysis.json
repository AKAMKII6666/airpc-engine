{
  "schemaVersion": 1,
  "analysisRunId": "preflight-analysis-1784789738114-5f4d137f",
  "role": "block-analyzer",
  "kind": "INDEX_SCHEMA_CORRUPTION",
  "recoverable": true,
  "confidence": "high",
  "rootCause": "执行索引 frontmatter 未在机器字段后立即用 --- 闭合：L82 写成 Markdown 标题 `## workflow_dir:`（YAML 中整行被当作注释，workflow_dir 未生效），随后正文标题与 `> **用法：**` 等 Markdown 被吸入 YAML；splitFrontmatter 把更后方的水平线 ---（约 L96）误判为闭合，导致 yaml 解析在 `> **用法：**` 处以 Implicit keys need to be on a single line 失败（EXFILE_BAD_YAML）。与同目录合格索引（如引擎存取Port抽象_执行索引.md）对比，正确形态应是 `workflow_dir:` + `adapter: table` 后紧跟闭合 ---，标题/用法块写在闭合之后。",
  "requiredPaths": [
    "docs/AI和人类/里程碑/v2.0/语音留言改造_执行索引.md"
  ],
  "scopeEvidence": "PREFLIGHT 阶段 Active task IDs 为空；阻断发生在 loadExFile/splitFrontmatter，尚无业务任务闭包。唯一需改路径即本 --exFile；属纯语法/frontmatter 契约修复，不改任务表产品语义、不触 gbx、不触应用源码。",
  "touchesGbx": false,
  "touchesExternalSystem": false,
  "dependencyAction": "none",
  "recommendedAction": "仅修该执行索引：1) 将 `## workflow_dir: .ai-workflow-v2-voicemail` 改为合法键 `workflow_dir: .ai-workflow-v2-voicemail`；2) 在 `adapter: table` 下一行立即写闭合 `---`；3) 把 `# Studio V2.0 · 语音留言改造执行索引` 与 `> **用法：**` 等人类可读正文保留在闭合 --- 之后（可对照 examples/minimal-execution-index.md 与同目录其他 *_执行索引.md）。不要改任务行语义或 verify/read_first 口径。",
  "humanReason": ""
}
