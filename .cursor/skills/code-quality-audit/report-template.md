# 报告模板（复制填空）

Agent 写入 `docs/AI与人类/归档/代码审查/第{N}次代码审查结果.md` 时使用。删除本说明段与「（填空）」提示。

---

```markdown
# SlimVID 第{N}次代码规范评审报告

> **文档层级：** AI与人类  
> **状态：** 归档  
> **读者：** 工程  
> **记录日期：** YYYY-MM-DD  
> **审查范围：** （填空，如 `app/`、`tests/`、`scripts/`；本轮是否改代码：否）  
> **评审依据：** `.cursor/rules/`、`docs/AI文档/工程/codex-review-boundaries.md`、`AGENTS.md`  
> **前置审查：** [第{N-1}次代码审查结果.md](./第{N-1}次代码审查结果.md)

## 一、结论先行

（3–8 句：总体健康度、相对上次变化、最需优先处理的 3 类问题、是否发现 V1 P0 级新缺陷）

---

## 二、相对上次审查（Delta）

| 上次 backlog / 问题 | 本次状态 |
|---------------------|----------|
| （填空） | ✅ 已闭合 / ⏳ 仍开放 / 🆕 新增 |

---

## 三、已执行检查

| 检查 | 结果 | 说明 |
|------|------|------|
| `npm run check:rules` | | |
| `npm run check:production-imports` | | |
| `npm run lint` | | error/warning 计数 |
| `npm run typecheck` | | 若失败说明原因与替代验证 |
| `npm run test` / `npm run check` | 未执行 / | |

`check:rules` 通过 **不代表** lint、目录组织、UI toast、bis 反向依赖等均合规。

### lint error 明细（若有）

| 类别 | 文件 / 数量 | 判断 |
|------|-------------|------|
| | | P1 / P2 |

---

## 四、文件分类与目录组织

### 做得好的地方

| 方面 | 说明 |
|------|------|
| | |

### 问题清单

（P1–P3；含 bis→routes/com、types 落点、目录平铺热点；附建议目标形态时注明「独立批次迁移，勿在功能 PR 顺手搬目录」）

---

## 五、代码编写与分层

### 后端

**符合规则：**（ bullet ）

**问题表：**

| # | 问题 | 严重度 |
|---|------|--------|
| | | |

### 前端

**符合规则：**（ bullet ）

**偏差表：**

| # | 规则条款 | 现状 | 严重度 |
|---|----------|------|--------|
| | | | |

---

## 六、注释与类型

### 类型

| 项 | 评价 |
|----|------|
| frontEnd / backEnd 分离 | |
| `any` 使用 | |
| 其他 | |

### 注释

| 项 | 评价 |
|----|------|
| bis / com 模块头 | |
| server 模块头 / JSDoc | |
| registry 生命周期 | |

---

## 七、技术细节与测试缺口

| # | 问题 | 说明 |
|---|------|------|
| | | |

---

## 八、优先修复建议（Backlog）

| 优先级 | 动作 | 预期收益 |
|--------|------|----------|
| **P1** | | |
| **P2** | | |
| **P3** | | |

**本计划不纳入：**（如 businessBackend 门面拆分、V1 产品缺口、用户未要求的目录大迁移）

---

## 九、评审方法说明

- **已运行：**（命令列表）
- **未运行 / 未完整：**（及原因）
- **抽样范围：**（目录与模块）
- **对照文档：**（上一份报告、改进计划勾选状态）

---

## 十、后续动作

- 若需可执行 checklist： [第{N}次代码审查改进计划.md](./第{N}次代码审查改进计划.md)
- 修复执行：`@batch-execute …改进计划.md batch_size=3`
```

---

## 改进计划模板（write_plan=true 时）

```markdown
# 第{N}次代码审查改进计划

> **依据：** [第{N}次代码审查结果.md](./第{N}次代码审查结果.md)  
> **规则基准：** `.cursor/rules/`  
> **用途：** 分批改代码；每批完成后勾选；全部完成后可选更新规则示例

## 批次总览

\`\`\`text
批次 A — P1 …  → verify: lint + check:rules
批次 B — P2 …  → verify: typecheck
批次 C — P2 目录治理（独立 PR） → verify: check:production-imports + 相关 tests
\`\`\`

## 批次 A — …

| 状态 | ID | 任务 | verify |
|------|-----|------|--------|
| ⬜ | A-1 | | |

## 验收（全计划）

- [ ] `npm run lint` 0 errors（或仅登记 backlog 的 warnings）
- [ ] `npm run typecheck`
- [ ] `npm run check:rules`
```
