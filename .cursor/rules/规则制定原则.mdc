---
description: airpc-engine 规则制定与冲突裁决的元原则
alwaysApply: true
---

# 规则制定原则

此文件解释和裁决 `.cursor/rules/` 中的开发规则，不替代具体领域规则。

**导航与需求真源**：根目录 [AGENTS.md](../../AGENTS.md)、[docs/AI和人类/需求/00-文档索引.md](../../docs/AI和人类/需求/00-文档索引.md)、[docs/AI和人类/技术设计文档/00-技术设计索引.md](../../docs/AI和人类/技术设计文档/00-技术设计索引.md)。Rules 不写整改进度、长路径表或业务状态细节。

## 最高目标

所有规则服务于：让维护者更快理解、定位、调试并安全修改代码。

- 优先降低认知与调试成本，不追求最短 diff。
- 显式边界优于隐式约定与散落副作用。
- 修改聚焦当前任务；不顺手重构、格式化或补完无关功能。
- 模块职责、schema、验收：**只维护在 docs**，不在 rules 重复全文。

## Think Before Coding

出现以下任一情况，**先澄清或呈现选项，再写代码**：

- 需求有多种合理解读（改动落在引擎 / Studio / docs 哪一层不清晰）。
- 跨柱改动但用户未指明落点（新字段进 Content、Profile、CallCard 还是 World）。
- 可能与 `docs/AI和人类/` 铁律冲突（通话中写存档、引擎依赖 React、Relay 子系统等）。
- 成功标准模糊（「优化一下」无可验证检查项）。

明显 trivial 任务（用户已给出精确文件与行号）可跳过冗长准备，但不得扩大改动范围。

### 编码前最小准备

1. **读上下文**： [AGENTS.md](../../AGENTS.md)、本任务相关需求文档、本任务触发的领域 mdc；非 trivial 按 [docs-execution.mdc](./docs-execution.mdc)。
2. **显式假设**：一句话复述意图；改动落点（engine / studio / docs / tests）。
3. **有歧义时呈现选项**：做法、影响范围、与规则是否一致、主要风险；给推荐及理由。

### 主动 push back

- 方案把 React / DOM / Next 引进 `packages/rpg-engine`。
- 通话中直接 Effect，或工具绕过 `RuntimeExitCandidate`。
- 把经历态存成无 `userId` 的全局单例。
- 为单次使用新建抽象或「预留扩展」。
- 方案扩大 diff（顺手重构、删未要求处理的遗留代码）。

表述：**「更简单或更安全的做法是 X；若仍坚持 Y，影响是 Z」**。

## 规则等级

| 等级 | 含义 |
|------|------|
| **必须执行** | 写口、引擎纯度、存档隔离、质量门禁；不得绕过 |
| **默认执行** | 目录组织、协作模式；偏离须与周围代码一致且理由可理解 |
| **推荐实践** | 注释、局部风格；以可读与可调试为准 |

## 冲突裁决

1. 安全、数据正确性与存档隔离（按用户 Profile）。
2. 架构边界（引擎 vs Studio vs 壳端口）。
3. 可测试性与边界门禁（禁止引擎依赖 React 等）。
4. 文档真源：`docs/AI/` 代码事实 > `docs/AI和人类/` 已采纳需求 > `docs/人类/` 原始意图。
5. 局部风格统一。

若偏离默认规则，原因应能从代码或短注释中理解。
